# QA Report R2 — Localized regression audit of the R1 repairs

- **Tree under test**: `/home/claude/work/invest-portfolio` @ `30c9969` + uncommitted repair set (8 source/doc files + 4 test files modified, 2 test files added; `git status` list recorded below). Full suite green per repair-loop log `/home/claude/work/qa/pytest_final.txt` (Master-asserted 4,752 passed / 3 skipped; not re-run by QA per charter).
- **Method**: read every repair diff, then re-drove each R1 repro path plus adversarial probes on the repaired seams only. Fresh scratch DBs `scratch_demo_r2.db`, `scratch_schwab_r2.db` (plus the R1 `scratch_fees.db` for door-level probes with batch-undo isolation). Pristine DB untouched (sha `cd3bf007…` verified below). Server stopped at end.
- **Blueprint**: `/home/claude/work/blueprint/REMEDIATION_BLUEPRINT.md` — including §0's reclassification of my BUG-03 (accepted; see verdict) and §6b FIX-A1b.

## 1. Per-finding verdicts

| ID | R1 title (short) | Verdict | Decisive evidence |
| --- | --- | --- | --- |
| BUG-01 | Demo corpus unimportable (FU-D34); smoke tool crashes | **FIXED** | Regenerated corpus imports via its own locked order on a fresh DB with **zero workarounds**: cash 11/11 (incl. two generator-derived TWD funding rows with FU-D34 notes), fx **2/2 clean**, corporate actions **7/7 one pass**; `smoke_import.py` runs end-to-end **ALL CHECKS PASS** (realized 10,680.73; withdraw guard both sides; joint-overdraft still caught batch-wise as `withdraw_insufficient_balance`); `verify_demo_corpus.py` ALL PASS; README updated (賣超提示 7→4 with cross-file explanation; one-pass chains; F1/F4 現況 sections). EXPECTED_positions unchanged in substance: my oracle re-ran **107/107 checks, 0 mismatches** on the imported corpus incl. every EXPECTED row (`qa/audit/out/oracle_demo_r2.txt`), and cross-endpoint **119/119** (`out/cross_demo_r2.txt`). |
| BUG-02 | Manual §9.3 contradicts FU-D43a/FU-D34 | **FIXED** | Both manuals rewritten to the three-class structure (hard write gates / ack-able correction gates / soft transaction gate) with FU-D43a/FU-D34 named (en 5×, zh 10× mentions), §12.1 E16 narrowed in both, §12.3 `v1.9` row in both, arbitration consequence stated. All 7 newly-cited test node ids exist in the tree (grepped). Behavioural claims spot-verified live: exact-balance withdrawal **passes** (201), one-cent-over hard-rejects 422 `withdraw_insufficient_balance`, `ack_negative` **cannot** bypass the write gate (422 with ack sent), same-file fx chain (TWD→USD funding a same-file USD→TWD) previews ok and commits — the batch/E1a wording is accurate. |
| BUG-03 | (as filed: spurious sticky-oversell warns) | **RECLASSIFIED — accepted; adjacent defect FIXED** | Master's correction is right and I verified it: the transactions preview **is** same-file sibling-aware (probe T1: buy100+sell60 one file → both `ok`); the 4 remaining corpus prompts are the documented **cross-file** residue (post-action sells; README now says exactly that, and the one-click path passes `pending_actions`). The real adjacent defect (T2, silent select-narrowed oversell) is **fixed**: commit `select=[sell]` with the covering buy deselected → `200 {written 0, skipped 2}`, ledger untouched, **no silent −60** (`out/r2_seam_probes.txt`). |
| BUG-04 | Corporate-action chains need 2 passes; misdirecting message | **FIXED** | R1 repro re-run on a fresh DB: real-history `corporate_actions.csv` (GGR shape) commits **8/8 in one pass, zero rejected** (R1: 7+reject, second upload needed). Cross-date synthetic chain (EXCHANGE D → SPLIT D+1, one file) also one-pass with correct arithmetic (KTLA 40 → MRCA 120) and clean batch-undo. Guards intact: same-date dependent action still hard-rejects (strictly-before cut — a batch row never justifies itself; D15/E12 intersection also fires), lone split and missing-prerequisite chain still hard-reject, and for those the previously-misdirecting message is now accurate. Numbers unmoved: oracle **112/112** on the re-import (`out/oracle_schwab_r2.txt`); TABLE_B reconciliation byte-identical to R1 — 68 OK, same two documented aggregate deltas (mapping DRIP-rounding 9.27; FIFO-vs-WAC on open groups) (`out/compare_schwab_r2.txt`). |
| BUG-05 | Dividend door leaks Python internals; NET requires gross | **FIXED** | 9-case malformed-cell sweep: blank/non-numeric/NaN amounts, blank/malformed dates, unknown type, malformed **optional** cells (previously silently dropped — now loud), missing column → every reason a vetted zh sentence naming the column; zero English/internals (regex screen). §6.3 net-only `NET` row previews ok, commits, books gross=net and reaches the §6.3b post-close realized path (+80 verified, then undone); NET + non-zero withholding hard-refused with a corrective sentence (redirect to CASH); explicit `withholding=0` accepted (`out/r2_dividend_sweep.txt`). |
| BUG-06 | Statement same-day same-sign ordering | **FIXED** | Fix is the correct mechanism (ascending stable sort preserving `_ordered`, then `list.reverse()`; comment rewritten). Verified: 2025-10-08 block now shows end-of-day 33,583.87 **on top** (API + UI screenshot `qa/evidence/cash_stmt_sameday_order_r2_fixed.png`); the displayed order is a strict reverse of a valid chronology (per-row balance replay over the reversed list — **exact**, no tie tolerance — a check that failed in R1 and passes now, also embedded in `cross_endpoints` 119/119); mixed-sign same-day probe: debit above credit, end-of-day on top; pagination `limit=7` concatenation == unpaged (61 rows, across the day boundary); `current_balance`/`balances` unchanged; CSV export still oldest-first with terminal == pool. |

## 2. What was re-run / new probes (all green unless noted)

- **Corpus battery (fresh `scratch_demo_r2.db`)**: locked-order HTTP import (6 kinds, no workarounds) → oracle w/ EXPECTED 107/107 → cross-endpoints 119/119 → repo's own `smoke_import.py` + `verify_demo_corpus.py` ALL PASS.
- **Seam probes** (`qa/audit/r2_seam_probes.py`, 32 checks, every write reverted via batch undo; ledger counts verified restored 50/7): T1 pin; T2; select=[both]; duplicate re-commit under select (row-hash skip); unrelated deselect keeps the cover; lone-oversell informed ack still writes; **V6** (buy@−50 excluded from the sibling batch → sell surfaces 賣超 at preview, un-acked commit 422 `warnings_unacknowledged`, acked commit writes only the sell with the invalid buy in `rejected_rows`); **V7** cash parity; declared `short_sale` via CSV + select (short_cover +50 exact); `pending_actions_csv` composed with `select` (post-split sell clean, split lands one-pass, shares 50); A2 cross-date one-pass chain + strictly-before/same-date/lone/missing-prereq rejections.
- **Dividend sweep** (`out/r2_dividend_sweep.txt`): 15 door checks + ledger effect + undo.
- **Manual §9.3 claim probes**: exact-balance pass, over-balance hard, ack-no-bypass, same-file fx chaining; cited-test existence (7/7).
- **By-design spot checks (unchanged)**: fee engine TW 855/0 · MY 9.40/10.00 · Moomoo-US 5.58/15.12 via the real preview door; sticky acked oversell (2412: oversold, valuation blanked, shares 10) and XIRR blanked with named reason on the oversold book.
- **Real-data regression (fresh `scratch_schwab_r2.db`)**: full 662-row import (now one-pass), oracle 112/112, TABLE_B reconciliation identical to R1 (47/47 shares, closed groups Σdiff −0.0029).

## 3. New findings

**None.** No regression and no new defect surfaced in any probe. Two sub-bug observations, unchanged from R1 and explicitly deferred by the blueprint (Zero-Divergence): FU-D34/D43a batch-guard messages print sibling-adjusted 可用餘額; warn-tier preview rows carry `code: null`. One R1 observation is now moot in practice (the corpus README's prose cash figure derives from `verify_demo_corpus`'s wider event model incl. not-importable rows — both tools now print their own figures and both pass).

## 4. Repair-set inventory audited (uncommitted, per `git status`)

`docs/accounting-formula-manual.en.md`, `docs/accounting-formula-manual.md`, `portfolio_dash/api/routers/cash.py`, `portfolio_dash/api/routers/input_center.py`, `portfolio_dash/data_ingestion/corporate_action_import.py`, `portfolio_dash/data_ingestion/csv_import.py`, `portfolio_dash/data_ingestion/dividend_import.py`, `portfolio_dash/data_ingestion/validate.py`; tests modified: `tests/contract/test_cash_api.py`, `tests/contract/test_input_import_api.py`, `tests/data_ingestion/test_import_action_chain.py`, `tests/data_ingestion/test_import_siblings.py`; tests added: `tests/contract/test_import_commit_select_rederives_transactions.py`, `tests/data_ingestion/test_dividend_import_cells_zh.py`; plus the regenerated (git-ignored) demo corpus. Diffs read in full for the six fixes; implementation matches the blueprint semantics, including FIX-A2's cache-discipline reasoning (widened replay not cached on degradation; per-(batch,date) purity argument holds for the one sharing caller).

## 5. Convergence recommendation

**CONVERGED.** All six R1 findings verified fixed (BUG-03 via its accepted reclassification + the fixed adjacent T2/V6 defects), zero regressions across 300+ re-run checks on three datasets, corpus tooling self-verifies again, and the by-design money behaviors are byte-stable against R1 (oracle and TABLE_B outputs identical). Residual known non-bugs are documented and deferred by design.

## 6. R2 artifacts

Scripts: `qa/audit/r2_seam_probes.py` (new); parametrized `setup_demo_db.py` / `inject_prices.py` / `setup_schwab_db.py` (argv DB path). Logs: `qa/audit/out/oracle_demo_r2.txt`, `cross_demo_r2.txt`, `r2_seam_probes.txt`, `r2_dividend_sweep.txt`, `oracle_schwab_r2.txt`, `compare_schwab_r2.txt`. Evidence: `qa/evidence/cash_stmt_sameday_order_r2_fixed.png`. Inline probe outputs (statement ordering/pagination/export, §9.3 claims, smoke/verify runs) recorded in the audit transcript and summarized above.
