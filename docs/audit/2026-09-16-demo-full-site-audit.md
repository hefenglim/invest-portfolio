# portfolio-dash 全站功能點擊測試報告

| 項目 | 內容 |
| --- | --- |
| 受測站台 | `<demo-url>`（demo／合成資料；實際網址在 git-ignored 的部署筆記） |
| 版本 | **v0.1.28 · 85dfafd**（未發行） |
| 測試時間 | 2026-09-15 23:5x – 2026-09-16 01:14（Asia/Taipei） |
| 測試方式 | 真實瀏覽器（Claude 內建瀏覽器，1440×900／1257×900／390×844）逐頁點擊＋表單送出＋API 回應比對＋後端原始碼交叉定位 |
| 授權範圍 | 使用者核可「完整寫入測試」（含新增／編輯／刪除），所有寫入均已於測試後還原 |
| 涵蓋頁面 | index · trades · dividend-inbox · cash · instruments · insights · pipeline-hub · news · data-center · settings（8 分頁）· login · input/ledger（轉址頁） |

---

## 一、總結

| 面向 | 結果 |
| --- | --- |
| Console／Page error（正常操作） | **0 筆**（唯一一筆 `e.target.closest` 為合成事件觸發，見 L2） |
| HTTP 非 2xx（正常操作） | **0 筆**；異常輸入正確回 400（日期區間顛倒、必填缺漏） |
| 金額恆等式驗證 | **93 項全數通過**（9 項總表恆等式 ＋ 14 檔 × 6 項逐檔恆等式） |
| 寫入／刪除還原一致性 | **3 條路徑全數逐位元還原**（交易、股利、換匯） |
| 匯出端點 | **15/15 全部 200**，檔名／Content-Disposition／內容皆正確 |
| RWD 橫向溢位（390px） | **0 處** |
| 待修問題 | **高 1 · 中 9 · 低 27** |

整體結論：**計算核心可信、寫入／重建路徑正確、匯出與錯誤處理紮實**。發現的問題集中在 **前端顯示語意、跨頁文案一致性、以及匯率資料源的三角一致性**，沒有任何一項會造成帳本被寫壞。


## 〇、修復狀態總覽（2026-09-16 補記）

> 本節與各項目下方的「修復狀態」區塊由修復作業補記；上方審計原文未改動。
> 分支 `feat/corporate-actions`，已 push（`909f0b2`／`486bbb8`／`7445208`，未併 main）；demo 站部署於 `7445208` 並驗證通過（`verify_live` ALL PASS、真瀏覽器走查 0 錯誤）。demo 資料修正：L26（0056 產業）與 L27（重複列，裁定 2(a)）。
> 狀態同步至 2026-09-17：§七的七題全數裁定並執行（§八），§七**無待決事項**；複驗回覆的 Q1 已於同日裁定並兩批執行完畢（§十.5），**無待決事項**。

> **複驗（2026-09-17 01:5x–02:4x，Asia/Taipei）**：由原稽核者在 demo 站（`v0.1.28 · 7445208`）以真實瀏覽器逐項重放原始重現步驟，佐以 API 回應與原始碼交叉確認。
> 結果：**34 項驗收完成**（含 2 項確認為非缺陷）、**1 項未通過（M9）**、**2 項部分通過（L5／L8）**。回歸無退化：94 項金額恆等式全過、匯出 15/15、390px 五頁（含設定 8 分頁）0 溢位、正常操作 0 console error。逐項證據見各項目下方的「驗收」區塊與 §九。

> **複驗回覆（2026-09-17，修復作業補記）**：M9／L5／L8 三項複驗意見逐一比對原始碼與 demo 資料庫（唯讀 SELECT，走 `vm_exec.py`）後**全部成立**，已依「修類別、不動已驗收項目」的原則修復；M9 的根因比複驗所述更深（**149/149 張卡**從未存過變數快照，不只舊卡）。修法、邏輯與驗證見 **§十**。本輪 commit（`cbfac0a` 複驗三項＋Q1 第一批、`55e0216` Q1 第二批、`86a6d6d` 報告同步）已 **push**（`7445208..86a6d6d`，未併 main），**demo 已部署於 `86a6d6d` 並站外驗證通過**（§十.8）；M9／L5／L8 的修復待原稽核者在 demo 上第二次複驗並於本報告加註。

| 狀態 | 項目 | 複驗結果（2026-09-17） |
| --- | --- | --- |
| ✅ 已修復（程式） | H1、M1、M2、M3、M4、M5、M6、M7、M8、M9、L1、L2、L3、L4、L5、L6、L7、L8、L9、L10、L11、L12、L13、L14、L16、L17、L18、L20、L21、L22、L23、L24、L25 | **除 M9／L5／L8 外全部驗收完成**；M9 ❌ 未通過、L5 ⚠️ 部分、L8 ⚠️ 部分 |
| ✅ 已修復（demo 資料） | L26、L27（重複列已依裁定 2(a) 刪除） | ✅ 兩項皆驗收完成 |
| ℹ 非缺陷（demo 真瀏覽器實測確認，裁定 5(b)） | L15、L19 | ✅ 兩項皆複驗成立（原稽核判斷有誤，已更正） |
| ✅ 已裁定並執行（§七 → §八） | M1（1b：MYR/TWD 改推導）、L13（3b+d：代號型不自動送＋開關）、L27（2a：刪除重複列）、M3（4a：demo 排程器維持關閉）、M9（7：接受預設） | ✅ 裁定 1/2/3/4 已在站上生效並驗收；裁定 7 的預設值造成 M9 未通過（見 §九） |
| ❌ 複驗未通過 → **✅ 已依複驗修復（§十.1）** | **M9** | `unverified_figures` 從未觸發；`unknown_symbols` 誤報率 ~98% → 母體改為「實際提示詞內的數字」（新欄 `insights.prompt_figures`，`input_snapshot` 不動）、新增第三態「無快照可核」、代碼規則改為名單＋形狀、徽章依命中分三種 |
| ⚠️ 複驗部分通過 → **✅ 已依類別修復（§十.2／10.3）** | **L5、L8** | 點名的實例已修，同類問題仍在 → L5：前端 25 處（11 檔）＋點名的後端 2 處全改全形，新增靜態守門測試（現為 0 處）；L8：標籤推導移為 `names.js` 單一定義，換匯／出金入金下拉同步 |
| ❓ 待你決策 | 無（0 項）— Q1 已裁定「分批」，兩批皆已完成：第一批 57 處使用者可見訊息、第二批更新紀錄目錄 29 處，後端守門現為 0 處（§十.5） | — |

---

## 二、待修問題清單

### 🔴 高（H）

#### H1 — 再平衡試算：目標合計剛好 100.00% 仍顯示「⚠ 目標合計超過 100%」

- **現象**：在「再平衡試算」把 3008 從 58.5 改成 58.4，footer 顯示「目標合計 **100.00%**」，但下方仍掛著紅字「⚠ 目標合計超過 100% — 請下調部分標的」。
- **重現步驟**：儀表板 → 持倉明細 → `⚖ 再平衡試算` → 任一標的目標值 −0.1，使合計為 100.0。
- **測量證據**（實際攔截的 request body）：
  ```json
  {"targets":{"1155":"0.006999999999999999","2317":"0.004","2330":"0.179",
   "2603":"0.01","2884":"0","3008":"0.584","5225":"0.006999999999999999",
   "8299":"0.111","AAPL":"0.026000000000000002","MSFT":"0.024","0056":"0.008",
   "TSLA":"0.036000000000000004","NVDA":"0.004"}}
  ```
  回應 `summary.over_allocated: true`。
- **根因（已在前後端兩側確認，非推測）**：
  1. 前端 `web/rebalance.js:220` — `state[sym] = Math.max(0, Number(inp.value) / 100 || 0)`，以 **JS float** 計算比例，`0.7/100` 序列化成 `"0.006999999999999999"`、`3.6/100` 成 `"0.036000000000000004"`。
  2. 後端 `portfolio_dash/strategy/rebalance.py:198` — `over_allocated = submitted_sum > _ONE`，**完全沒有容差**。
  3. 這些 float 殘渣以 Decimal 相加後為 `1.000000000000000004 > 1`，於是旗標為真。前端的保險絲 `sumTarget > 1.0001`（`rebalance.js:293`）反而正確判為 false，所以畫面上寫著 100.00% 卻同時報錯。
- **建議**：前端改送定點字串（`(pct/100).toFixed(6)`），**並且**後端加容差（`> Decimal("1.0001")`）。兩邊都補才不會下次從另一個方向重演。同時這也是 `CLAUDE.md` 不變量 3「金額不走 float」的精神漏洞。

> **修復狀態：✅ 已修復**
> 修法：前後端各補一邊，兩邊都補。`web/rebalance.js`：目標權重改以「整數・千分之一（0.1%）」為狀態，送出字串由整數直接拼出（不再 `String(float)`），前端 Σ>100% 檢查改用整數相加；`strategy/rebalance.py`：`over_allocated = Σ > 1 + 0.0001`（容差，並註明 1 dp 欄位最小超額為 0.001，容差永遠遮不住真超額）。測試：用報告攔截到的那組 body（Σ=1.000000000000000004）斷言 `over_allocated is False`，100.1% 仍為 True；另有靜態契約測試禁止 `Number(inp.value)/100` 與 `1.0001` 回流。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> 重放稽核原文攔截到的 request body（Σ=1.000000000000000004）→ `over_allocated: false`；真實超額（0.5+0.5+0.001）→ `true`，容差沒有遮住真超額。前端送出的 body 已是定點字串（`"0.007"`／`"0.182"`／`"0.030"`），float 殘渣消失；後端 `rebalance.py:222` 為 `submitted_sum > _ONE + _OVER_TOLERANCE`（`_OVER_TOLERANCE = Decimal("0.0001")`）。UI 側：開啟即「目標合計 100.00%」且無警示；手動改成剛好 100.0 仍無警示；改成 101.00% 才出現警示。

---

### 🟠 中（M）

#### M1 — 三組匯率彼此不三角一致，換匯會讓報告幣別淨值憑空變動

- **現象**：`USD/TWD`、`USD/MYR`、`MYR/TWD` 三個報價各自獨立儲存，彼此不滿足 no-arbitrage。
- **測量證據（重新整理報價後、三組匯率皆為 2026-09-15 且 `stale:false`，非過期造成）**：

  | 幣對 | 系統值 |
  | --- | --- |
  | USD/TWD | 31.834999 |
  | MYR/TWD | 7.803300 |
  | USD/MYR | 4.082500 |
  | **推導 USD/TWD**（4.0825 × 7.8033） | **31.856972** |
  | **落差** | **+0.0690%** |

- **可觀察後果**：實測在 Moomoo MY 帳戶用即期價把 4,000 MYR 換成 983.28 USD，`/api/cash.reporting_total` 從 `25,404,962.183375760` 變成 `25,404,921.306279040`，**憑空少了 40.88 TWD**。刪除該筆換匯後金額逐位元還原，證明差額完全來自匯率換算路徑，不是帳務錯誤。
- **影響**：馬股／美股部位的報告幣別數字會因換算路徑不同而有 0.05–0.13% 的差異；換匯這個本應「報告幣別價值中性」的動作會產生幻影損益。
- **建議**：指定一個 base（例如 USD），另兩組由 cross rate 推導；或在 `pricing/` 層加一道三角一致性檢核並在「資料新鮮度」標示落差。

> **修復狀態：✅ 已修復（檢核＋揭露；資料源依裁定 1(b) 改為推導）**
> 修法：後端 `portfolio/dashboard.py` 在組 `freshness` 時，對已儲存的三組匯率做三角檢核：`implied = USD/MYR × MYR/TWD`、`direct = USD/TWD`、`gap_pct`，`|gap| ≤ 0.05%` 才算 `ok`，以 `freshness.fx_triangulation[]` 回傳（Decimal 字串，匯率本身不動）。前端 `web/app.js` 在「資料新鮮度」面板以琥珀色註記落差與原因。用報告的實測值做單元測試（推得 31.856972、落差 0.0690%、`ok=False`）。**要不要指定 base 幣別、把另兩組改成 cross rate 推導**會改動報告幣別數字，留給你決策（見文末問題 1，含 0.05% 門檻是否合適）。裁定後補記（1(b)，已實作）：`pricing/cross.py` 在每次 FX 寫入後推導 `MYR/TWD = USD/TWD ÷ USD/MYR`（`source=derived:USD`，涵蓋整段歷史），抓取清單移除 MYR/TWD，新鮮度面板標「推導」；demo 執行一次更新報價後 1,346 列改為推導列，三角落差 0.0000%。0.05% 門檻不動。附帶：spec-17 golden payload 因新增欄位重新基線（+14／−1 行，只有這兩個新鍵），其合成匯率本身三角落差 −3.75%，golden 因此記為 `ok:false`，是誠實的回歸錨點。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> `freshness.fx_triangulation` = `implied 31.747998 / direct 31.747999 / gap_pct -0.0000 / ok:true`；`freshness.fx` 中 MYR/TWD 的 `source` 為 `derived:USD`；資料新鮮度面板顯示「MYR/TWD **推導**」與「匯率三角一致性 … 落差 -0.0000%（一致）」。**獨立重算**（以 `currency_view` 的原幣／報告幣比值反推）落差 **0.000001%**（稽核當時 0.0690%）。`POST /api/actions/refresh-quotes` 回應內含 `derived:USD: MYRTWD`，證實每次抓取後都會重推。

#### M2 — 洞察管線卡「② 輸入」節點在非 per_symbol 任務顯示無意義的標的數

- **現象**：`持倉週報`（scope=portfolio）顯示「**0 檔標的**」，`市場週報`（scope=per_market）顯示「**3 檔標的**」，但兩者底下都列出 14 個代號。
- **證據**：`GET /api/insight-tasks/status` 直接回 `input.text: "0 檔標的"` / `"3 檔標的"`，故問題在後端。
- **根因（已定位到行，CONFIRMED）**：`portfolio_dash/llm_insight/pipeline_status.py::_input`
  ```python
  count_text = f"{len(f.universe_symbols)} 檔標的"
  if f.missing_or_stale_symbols:                       # ← warn 分支
      return NodeState(lv="warn", text=count_text, ...)  # line 93：直接用 count_text
  if f.removed_recently:                                # ← info 分支
      return NodeState(lv="info", text=count_text, ...)  # line 96：同樣
  text = count_text if f.scope == "per_symbol" else "全持倉"   # line 97：只有 ok 分支才分 scope
  ```
  `"全持倉"` 這個 scope-aware 標籤**只寫在 ok 分支**。只要有任何一檔缺價／過期（在本機是常態），就會走 warn 分支印出對 portfolio／per_market 毫無意義的數字。
- **附帶**：portfolio／per_market scope 的代號清單有 **AAPL 出現兩次**（依部位列出、未去重），per_symbol scope 則正確去重為 13 檔。
- **建議**：把 scope 判斷提到三個分支之前；清單改用 instrument 去重。

> **修復狀態：✅ 已修復**
> 修法：`llm_insight/pipeline_status.py::_input`：scope 判斷提到三個分支之前——per_symbol 顯示「N 檔標的」、portfolio／on_alert 顯示「全持倉」、per_market 顯示「N 個市場」，warn／info 副標照舊；`api/insight_service.py` 新增 `_price_population`：`data.holdings` 每個（帳戶, 標的）一列，以 `dict.fromkeys` 去重保序（AAPL 兩帳戶只列一次）。`test_pipeline_status.py` +9、`test_pipeline_hub_api.py` +5（各 scope 的標題、去重、兩介面一致）。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> `/api/insight-tasks/status` 與管線卡 UI 皆為：持倉週報「全持倉」／個股健檢「13 檔標的」／市場週報「3 個市場」／持倉建議與提點「13 檔標的」／持倉提點「全持倉」。原始碼確認 `pipeline_status.py::_input_head()` 已把 scope 判斷提到 warn／info／ok 三個分支**之前**。附帶的 AAPL 去重需要「有標的缺價／過期」才會出現在副標，**今日 13 檔報價全新鮮**，故以原始碼（`insight_service.py::_price_population` 以 `dict.fromkeys` 去重）確認，未在畫面上重現。

#### M3 — 排程器未運行，但介面沒有任何提示，且過期產出被當成「今日」呈現

- **現象**：
  - 設定 → 排程中心：**25 個工作全部顯示「啟用」，但「下次執行」全部是「—」**（`/api/scheduler/jobs` 回 `enabled:true, next_run_at:null`）。
  - 儀表板「**今日摘要**」內容產生於 **2026-07-22 13:45**；「週行動清單」產生於 **2026-07-15 17:41**——距今近兩個月，卡片標題仍寫「今日／本週」，只有小字標註產生時間。
  - 「月度成績」只有 **2026-07** 一列（`portfolio_snapshots` 僅 1 筆），8、9 月缺漏。
  - 最後一次資料庫備份：**2026-07-18**（`freshness.last_backup_at`）。
- **影響**：使用者無從得知「為什麼摘要停在 7 月」；若正式站也發生排程停擺，畫面看起來完全正常。
- **建議**：(a) 新增排程器整體狀態指示（running / disabled）；(b) digest 卡片在內容超過 N 天時改標題或加「過期」徽章，與報價的 `stale` 處理一致。

> **修復狀態：✅ 已修復**
> 修法：(a) `GET /api/scheduler/jobs` 新增頂層 `scheduler: {running, reason}`；設定 → 排程中心在 `running=false` 時顯示琥珀色橫幅「排程器未啟動 — 所有排程工作都不會自動執行（「下次執行」因此皆為 —）；仍可手動執行」；(b) `freshness.scheduler_running` 由 router 餵入（同 `last_backup_at`），儀表板「資料新鮮度」面板據此提示；(c) `web/digest.js` 對 `generated_at` 超過門檻（今日摘要 > 2 天、週行動清單 > 8 天）的卡片加「已過期・N 天前產生」標記。`freshness.scheduler_running` 三態：true／false／null（null＝非 HTTP 呼叫、未知），絕不把「未知」報成「停用」。demo 站本來就以 `PD_DISABLE_SCHEDULER=1` 執行（設計如此，資料才可重現），現在畫面會說出來；門檻 2／8 天與「demo 要不要開排程器」見問題 4（裁定 4(a)：demo 排程器維持關閉、門檻不動）。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> (a) `/api/scheduler/jobs` 回 `scheduler: {running:false, reason:"PD_DISABLE_SCHEDULER=1"}`，排程中心橫幅：「排程器未啟動 — 本實例的所有排程工作都不會自動執行（「下次執行」因此皆為 —）；仍可用「立即執行」手動觸發　原因：PD_DISABLE_SCHEDULER=1」。(b) `freshness.scheduler_running = false`，資料新鮮度面板印出對應說明。(c) 儀表板兩張卡：今日摘要「已過期・**56 天前產生**」、週行動清單「已過期・**63 天前產生**」，兩者都附「排程器未啟動，摘要不會自動更新」。

#### M4 — 手動交易寫入成功後，「已持有」提示未刷新，且刷新持股打錯帳戶

- **現象**：在 `TW Broker` 寫入 2884 買進 100 股後（`POST /api/input/manual/commit → 201`，toast「✓ 寫入成功（#67）」），代號欄下方仍顯示「已持有 2884 玉山金控 **100 股 均價 93.20**」，正確值應為 **200 股 / 69.475**。
- **測量證據**：寫入後的自動刷新請求是 `GET /api/input/holdings?**account=moomoo_my**`，但當時 `#m-account.value === "tw_broker"`。
- **影響**：連續輸入第二筆時，操作者看到的是過時（且屬於別的帳戶）的持倉基準。
- **建議**：commit 成功的 refresh 讀取當前 `#m-account` 值。

> **修復狀態：✅ 已修復**
> 修法：`web/input.js::afterCommitRefresh` 原本只暖 `#d-account`（股利表單的帳戶下拉），所以請求打到 moomoo_my。現在同時暖 `#m-account`（交易剛寫入的帳戶），回來後若代號 picker 開著就重繪，已持有註記立即更新；股利表單的暖身保留。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> 在 `tw_broker` 寫入 2884 買進 100 股（`POST /api/input/manual/commit → 201`，#68）後，刷新請求為 **`GET /api/input/holdings?account=tw_broker`**（稽核當時是 `moomoo_my`），代號提示同步更新為「已持有 2884 玉山金控 **200 股 均價 69.20**」（9,320＋4,520＝13,840 ÷ 200＝69.20 ✓）。驗收後已刪除 #68，2884 還原為 100 股／93.20，交易數回到 56 筆。

#### M5 — 帳戶名稱雙軌：同一頁同時出現「台灣券商」與「TW Broker」

- **現象**：
  - 交易帳本頁 **篩選 chips**／**帳本列**／**編輯彈窗**：`台灣券商`、`嘉信 Schwab` ✅
  - 同一頁 **手動交易帳戶下拉**：`TW Broker（TWD）`、`Charles Schwab（USD）` ❌
  - **CSV 匯入預覽表**：直接印 `tw_broker`、`schwab` ❌
  - **資金管理 › 各帳戶現金池**：`Charles Schwab`、`TW Broker` ❌
  - **收件匣**：`3008 LARGAN（TW Broker）`、`2026-07 折讓款（TW Broker）` ❌
- **根因（專案自身已追蹤）**：`web/names.js` 是唯一真相來源，但檔頭註解明確寫著 `cash.js / corp-action-form.js / input.js` 仍列在 `tests/contract/test_account_name_single_source.py` 的 `_PENDING` 清單中。收件匣（`inbox.js` / `rebate-inbox.js`）看來也未納入。
- **建議**：把這三支（＋收件匣）接上 `pdNames.account()`，並把它們從 `_PENDING` 移除。

> **修復狀態：✅ 已修復**
> 修法：六支檔案全部接上 `window.pdNames.account(row.account_id)`（每一列 API 都已帶 `account_id`）：`cash.js`（換匯帳本列、現金池卡、透支橫幅、收支明細副標、資金紀錄列、編輯／刪除對話框）、`input.js`（CSV 預覽表帳戶欄、交易與股利兩個帳戶下拉）、`corp-action-form.js`、`rebalance.js`（帳戶 chips 與 legs）、`rebate-inbox.js`（5 處）、`inbox.js`（3 處）。`cash.html` 與 `dividend-inbox.html` 補載 `names.js`。`tests/contract/test_account_name_single_source.py` 的 `_PENDING` 清空，任何新表再印英文帳戶名會直接紅燈。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> 逐頁掃描 rendered text：交易輸入帳戶下拉、CSV 匯入預覽「帳戶」欄、資金管理現金池與換匯帳本、收件匣（配息卡與折讓款卡）**全部為中文帳戶名**；四頁合計 `TW Broker`／`Charles Schwab`／`tw_broker`／`schwab` 出現 **0 次**。

#### M6 — 個股詳情抽屜不進 history：無法分享連結、返回鍵不關閉

- **現象**：
  - 點持倉列開啟抽屜後 `location.hash` 仍為 `""`，網址列毫無變化 → 無法把「2330 的詳情」分享給別人或加書籤。
  - 抽屜開啟時按瀏覽器**上一頁**：網址變了（`/index.html`），**抽屜卻沒關**，等於白白消耗一個歷史紀錄；手機上使用者的直覺（返回＝關閉浮層）失效。
  - 反向路徑是通的：手動把網址改成 `#sym=AAPL` **會**正確開啟抽屜（週報卡片的「前往」連結就是靠這個）。
- **建議**：開啟時 `history.pushState('#sym=XXXX')`，`popstate` 時關閉抽屜。

> **修復狀態：✅ 已修復**
> 修法：`web/detail.js`：使用者開啟抽屜時 `history.pushState({pdDrawer:true}, '', '#sym=XXXX')`（網址可分享／加書籤）；`popstate` 時若 hash 不再是 `#sym=` 就關閉抽屜（上一頁＝關閉）；用 ✕／Esc／背景關閉時 `history.back()` 把那一筆彈掉，不留幽靈歷史；←／→ 切換標的用 `replaceState`（一個抽屜一筆）；由 deep link 或前進／後退開啟的抽屜不再 push。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> 開啟抽屜 → `location.hash = "#sym=2330"`、`history.length` +1（可分享／加書籤）；按**上一頁** → 抽屜關閉且 hash 清空；以 ✕ 關閉 → hash 清空且 `history.length` 相對基準 **Δ=0**（無幽靈歷史）；deep link `#sym=AAPL` 仍正常開啟；Esc 關閉並清 hash。六個情境全通過。

#### M7 — 換匯中心：手動填入「買入金額」後，試算提示永遠停在「試算中…」

- **現象**：`#cfx-estimate` 顯示「試算中…」且不會自行結束，即使 `/api/cash/fx-estimate` 已多次回 200（`{"available":true,"estimate":"983.28","rate":"0.245821","rate_as_of":"2026-09-09"}`）。必須手動按旁邊的「重新試算」，文案才會換成「以 2026-09-09 匯率 0.2458 試算，可依實際成交調整」。
- **根因（推測待驗）**：原始碼註解寫著這是「manual-override re-run affordance」——手動覆寫後刻意不自動重算是合理設計，但**狀態文案沿用了「試算中…」**，等於向使用者宣告一個永遠不會完成的計算。
- **建議**：手動覆寫時把文案改成「已手動指定買入金額（按『重新試算』以即期匯率回填）」。

> **修復狀態：✅ 已修復**
> 修法：根因與報告的推測不同：不是文案設計，而是**競態**——使用者在試算請求還在飛時手動填買入金額，`runEstimate` 的 `!estPristine` 早退正確地丟掉答案，但沒有一起收回「試算中…」。`web/cash.js::markBuyEdited` 現在在 input 事件當下同步改寫說明為「已手動指定買入金額 — 按「重新試算」可依即期匯率回填」。既有的 pristine 不變量（e2e）不受影響。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成**
> 手動填入「買入金額」後，`#cfx-estimate` 立即變成「**已手動指定買入金額 — 按「重新試算」可依即期匯率回填**」，不再停在「試算中…」。

#### M8 — 乾跑預檢與管線卡對「價格資料」給出相反判定

- **現象**：同一個任務（持倉週報），
  - 管線卡「② 輸入」＝ **⚠ warn**，副標「…8299 缺價/過期」
  - 乾跑預檢 **R4 價格資料 ＝ ✓ 通過**
- **影響**：兩個都是給同一個人看的守門指標，卻互相矛盾，使用者不知道該信哪一個。
- **建議**：兩處共用同一個 R4 判定函式。

> **修復狀態：✅ 已修復**
> 修法：`api/insight_service.py::_price_state_for` 單一 helper 回傳（缺價, 過期）兩個集合，`_gather_facts`／`_preflight_saved`／`_preflight_draft` 三處都從它讀；`PipelineFacts.missing_or_stale_symbols` 拆成 `missing_price_symbols`＋`stale_price_symbols`（另發現：儀表板把缺價也標 stale，helper 明確排除以免重複點名）。語意對齊而非抹平：缺價 → 兩邊都是 R4 warn（零成本異常卡，閘門行為不變）；僅過期 → 兩邊都是 info（卡片「價格過期：…」、乾跑多一條 `R4_stale_price` info，不改 verdict；`web/pipeline.js`／`pipeline-preflight.js` 本來就會渲染 info 等級，前端零修改）。契約測試證明同一 fixture 下兩個介面點名同一組標的。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：✅ 驗收完成（現況一致＋原始碼確認）**
> 今日 13 檔報價全新鮮，管線卡「② 輸入」無警告、乾跑預檢「R4 價格資料 ✓ 通過」，**兩介面一致**。原始碼確認 `insight_service.py::_price_state_for()` 為單一 helper，`_gather_facts`（L1093）／`_preflight_saved`（L1608）／`_preflight_draft`（L1653）三處共用，`missing_price_symbols` 與 `stale_price_symbols` 已拆開，並新增 `R4_stale_price` info 列。**對立情境（有標的過期）需人為製造過期資料才能重現，未在 demo 上製造**，故此項以「現況一致＋原始碼」結案。

#### M9 — AI 洞察卡數字尺度錯誤與幻覺代號

- **現象**（皆為畫面上現存的快取卡片）：
  - 2026-07-05 18:22 卡：「美股部位淨值 202.4 萬美元，**未實現獲利 429.1 萬美元**」
  - 2026-07-05 18:26 卡（同一批資料）：「未實現收益 **4,290.80 美元**，部位規模共 **11,951 美元**成本」
    → 同一組數字被放大 **1000 倍**。
  - 另一張卡提到「**LRDIM (6883)**」——此代號不存在於持倉或觀察清單。
- **佐證**：系統自己的「AI 戰績」頁已量化這個問題：量化命中率 **13.33%**、滾動校準缺口 **−46.60%**；儀表板鈴鐺也有「AI 校準誤差偏高 40.8pp」。
- **建議**：卡片產生後加一道「數字必須出現在餵入的變數快照中」的後置檢核（`llm_insight/` 已有變數表可比對），不通過就標記為「數值待核」而非直接顯示。

> **修復狀態：✅ 已修復（讀取時後置檢核，不阻擋）**
> 修法：新增 `llm_insight/figure_check.py`：把卡片文字（title＋summary＋body）裡的數字（含千分位、%、萬／億／千、K／M）與該卡儲存的 `input_snapshot` 變數快照逐一比對（相對容差 0.5%，考慮尺度變體），對不上的列為 `unverified_figures`；括號代號 `(6883)` 不在已註冊標的中列為 `unknown_symbols`。`GET /api/insights` 每張卡多帶 `figure_flags`，`insights.html` 顯示「數值待核」徽章與 tooltip「卡片中的數字未出現在餵入的變數快照中，請以儀表板數字為準」。用報告的兩個例子做單元測試（429.1 萬美元被標、4,290.80 不被標；(6883) 被標）。比對取絕對值（檢查的是尺度不是正負，「虧損 3,200」對上快照的 −3200 不誤報）；5 筆上限與 0.5% 容差為實作選擇（問題 7；裁定：接受預設）。舊卡片因快照仍在，讀取時同樣會被標。

> **驗收（2026-09-17，demo `7445208` 真瀏覽器複驗）：❌ 驗收未通過（兩個面向）**
> 詳見 §九「未通過項目」。摘要：(a) `unverified_figures` 在 **148 張卡片上觸發 0 次**，稽核點名的 #37「未實現獲利 **429.1 萬美元**」仍**沒有**徽章；(b) `unknown_symbols` 誤報率極高 —— 48 個代號實例中只有 **1 個**（6883）是真的，其餘 47 個是 PBR／MA20／MA60／MA120／MA200／MA50／RSI14／BUY／HOLD／KLCI／TAIEX 等指標與比率。

> **複驗回覆／修復（2026-09-17）：✅ 已修復（三個面向；詳 §十.1）**
> **(a) 母體從未存在，不只舊卡。** demo 唯讀查詢：149/149 張卡（最新 2026-08-25）的 `input_snapshot` 都是 `YYYY-MM-DD|標的` 這種 13～20 字元的指紋標籤——`generate.py::_snapshot_for()` 的 fallback，因為 `RunInputs.input_snapshots` 這個縫從來沒有任何呼叫者餵入；修復註記「舊卡片因快照仍在」是錯的，單元測試用合成快照證明了函式、沒證明系統。修法：在呼叫模型的同一處，把**實際送給模型的提示詞**內所有數字擷取為母體（`figure_check.prompt_figures_json(prompt)`），存進**新欄** `insights.prompt_figures`（`ensure_tables` 加欄，`DEFAULT ''`）；讀取端改比對此母體。`input_snapshot` 的值與語意不動——它餵快取指紋（改了會讓同日重跑失去快取命中而重複計費）與 Loop-2 master 提示詞。舊卡沒有母體 → 回傳第三態 `snapshot: "none"`（只在卡片有印數字時；純敘事卡與零 LLM 的資料異常卡為 `checked`），前端掛灰色「無快照可核」，不再是乾淨的 `[]`。
> **(b) 代碼規則改為名單＋形狀。** `_INDICATOR_RE = ^(MA|EMA|SMA|WMA|DMA|RSI|KD|KDJ|ATR|ADX|CCI|OBV|DMI|SAR|MFI|ROC|MACD|BOLL|BB)\d+$` 吃掉帶週期的變體；名單分組補上 PBR／PSR／PCF／DPS／BPS／ROIC／WACC／KLCI／TAIEX／TWII／TPEX／SPX／NDX／DJI／SOX／BUY／SELL／HOLD／LONG／SHORT 等；判斷順序先查註冊表（已註冊永不標）再查名單／形狀，`(6883)` 仍被標。附帶兩個在母體啟用後才會浮現的誤報類別一併堵住：括號代碼由 4 位放寬到 4～6 位（`(00878)` 不再被讀成數字 878）、卡片內裸寫的已註冊代號（「2330 量縮整理」）不算數字。
> **(c) 徽章依命中分三種。** 只命中數字→「數值待核」（紅）、只命中代碼→「未知代碼」（紅）、無母體→「無快照可核」（灰，新 CSS `.badge-unchecked`），tooltip 各自對應，矛盾的那句已移除。
> **測試**：以 #37 真實儲存形態（`"2026-07-05|US"`）為輸入的回歸測試（斷言 `none`、非乾淨）；走真實產生路徑（stub 模型）在**儲存列**上斷言「429.1 萬」被標、提示詞內的數字不被標、`input_snapshot` 仍是指紋標籤；欄位遷移往返（舊表加欄）；指標／比率／評等／指數不是代碼、5～6 位代碼不是數字；契約測試更新（wire 多 `snapshot` 鍵、舊列在 `/api/insights` 回 `none`、頁面三種徽章文案存在）。

---

### 🟡 低（L）

| # | 位置 | 問題 | 修復狀態／修法（2026-09-16） | 驗收（2026-09-17） |
| --- | --- | --- | --- | --- |
| L1 | 儀表板 · 持倉明細 | 「市值」欄**以報告幣別換算值排序**，但顯示的是原幣金額，畫面上看起來像沒排序（41,321 USD 排在 343,500 TWD 上面）。相鄰的「未實現損益」則是**分幣別分組排序**且有標示「（分幣別）」，兩欄語意不同卻只有一欄說明。 | **✅ 已修復** — 「市值」欄啟用排序時表頭顯示「（依報告幣別）」，與「未實現損益」的「（分幣別）」對稱；tooltip 加註「顯示的是原幣金額」。 | **✅ 驗收完成** — 排序啟用時表頭為「市值▼（依報告幣別）」，與「未實現損益▼（分幣別）」對稱；tooltip：「依報告幣別市值排序（＝權重順序）；顯示的是原幣金額」。 |
| L2 | `web/app.js:1292` | `document.addEventListener('keydown', e => { if (e.target.closest(...) })` 未防 `e.target` 非 Element；以合成事件可穩定觸發 `TypeError: e.target.closest is not a function`。真實鍵盤操作不易觸發，屬健壯性缺口。 | **✅ 已修復** — `app.js` 與 `detail.js` 的 keydown 都先 `e.target instanceof Element` 再 `.closest`。 | **✅ 驗收完成** — `app.js:1360` 已改為先判斷 `e.target instanceof Element`（不是 Element 就直接 return），再呼叫 `.closest(...)`；重放稽核的原始重現（對 `document` 發 Escape／j／k／ArrowDown／Enter 合成 keydown）**0 錯誤**。 |
| L3 | 全站 toast | 錯誤 toast 不會自動消失，也不隨條件解除而清掉：測試中一則「✕ 帳本資料載入失敗 日期區間無效」在日期已重設後仍殘留數分鐘；成功 toast 則會自動消失。 | **✅ 已修復** — `shell.js` toast：fail／warn 30 秒後自動消失（hover 暫停計時），同文字的 toast 取代舊的一則而非疊三層。 | **✅ 驗收完成** — 同一則錯誤連續觸發 4 次，畫面只保留 **1 則** toast（不疊層）；自最後一次觸發起約 **21–24 秒**自動消失（逐 3.5 秒取樣）。成功 toast 仍約 4 秒消失。 |
| L4 | 資金管理 | 分頁名稱「**賬**戶現金」用了非台灣正體用字，同頁其他處皆為「**帳**戶」。 | **✅ 已修復** — 全站「賬」→「帳」：`cash.html`／`cash.js`（分頁名、出金說明、註解）、後端驗證訊息（`validate.py` 的「帳戶現金 −495000」）、what's-new 文案，以及斷言這些字串的測試；grep 全站剩 0 處。 | **✅ 驗收完成** — 資金管理整頁 rendered text 中「賬」出現 **0 次**，分頁名為「帳戶現金」。 |
| L5 | 收件匣／新聞庫／管線中心 | 中文文案半形全形標點混用：`配息／配股偵測,台美馬全市場`、`排程:每日 06:00(設定→排程 可調)`、`偵測是持續進行的:每日排程掃描,…`。 | **✅ 已修復** — `dividend-inbox.html` 兩句、`news.html` 排程提示改為全形標點。 | **⚠️ 驗收部分通過** — 稽核**點名的兩句已修**（「配息／配股偵測，」「偵測是持續進行的：…」「排程：每日」）；但同類問題仍在：收件匣折讓款區塊 **16 處**、新聞庫 2 處、再平衡 footer、資金管理合併現金列、`news.js`／`ledger.js` 各 1 處。詳見 §九。 **→ 2026-09-17 已依類別修復（§十.2）**：掃描器在 `web/` 找到 25 處字串（11 檔）全改全形；複驗點名的「ledger.js 確認框」那句其實來自後端 `validate.py`（`晚於今日,確認無誤?`），與 `改記為分割(SPLIT)` 按鈕標籤（`ledgers.py`，前端只是 fallback）一併改；新增 `tests/contract/test_zh_punctuation_fullwidth.py` 靜態守門（文字節點＋屬性＋JS 字串常量，排除註解）= 0 處。 |
| L6 | 換匯中心 | 「隱含匯率」的報價基準與帳本列相反。`MYR 4000 → USD 1000` 顯示「1 USD = 4.0000 MYR」（與帳本一致 ✅）；`USD 1000 → MYR 4000` 卻顯示「1 MYR = 0.2500 USD」，而寫入後帳本列會是「1 USD = 4.0000 MYR」。 | **✅ 已修復** — 統一以「較值錢的幣別為 1」報價（rate ≥ 1，與市場慣例一致：USD/TWD、USD/MYR、MYR/TWD）。後端 `StoredFxConversion.implied_quote` 決定方向，`/api/ledgers/fx` 多帶 `implied_unit_ccy`／`implied_per_ccy`（`implied_rate` 值 ≥ 1），列印報表同規則；表單即時試算 `updImplied` 同規則。契約測試：USD→MYR 與 MYR→USD 兩列都顯示 1 USD = 4.0000 MYR。 | **✅ 驗收完成** — `MYR 4000 → USD 1000` 與 `USD 1000 → MYR 4000` **兩個方向都顯示「1 USD = 4.0000 MYR」**；`/api/ledgers/fx` 帶 `implied_unit_ccy`／`implied_per_ccy`，`implied_rate` ≥ 1（實測 32.3206，unit=USD、per=TWD）。 |
| L7 | 換匯中心 | 把「買入幣別」設成與「賣出幣別」相同時，系統會**靜默改寫賣出幣別**（MYR→MYR 變成 USD→MYR），沒有任何提示，可用餘額也跟著換掉。 | **✅ 已修復** — `cfx-to-ccy` 下方新增說明列：「買賣幣別不可相同 — 已自動將賣出幣別改為 USD，可用餘額也隨之切換」；下一次不需翻轉時自動隱藏。 | **✅ 驗收完成** — 把買入幣別設成與賣出相同時顯示：「買賣幣別不可相同 — 已自動將賣出幣別改為 USD，可用餘額也隨之切換」。 |
| L8 | 交易輸入／股利／換匯 帳戶下拉 | 以 `settlement_ccy` 標註 →「Moomoo MY（USD）」，但該帳戶的資金幣別是 MYR、且同時做馬股（MYR）交易，標註易誤導。 | **✅ 已修復** — 帳戶下拉改為 zh 名稱＋**實際交易幣別**（由綁定市場推得）：「Moomoo MY（USD／MYR）」「台灣券商（TWD）」「嘉信 Schwab（USD）」。 | **⚠️ 驗收部分通過** — 交易帳本的三個下拉已改為「Moomoo MY（USD／MYR）」「嘉信 Schwab（USD）」「台灣券商（TWD）」✅；但稽核原文明列的**「換匯」帳戶下拉**（資金管理 › 換匯中心 `#cfx-account`）與出金入金 `#cm-account` **仍為「Moomoo MY（USD）」**。詳見 §九。 **→ 2026-09-17 已依類別修復（§十.3）**：標籤推導移到 `names.js::pdNames.accountOption(a)`（單一定義），`cash.js` 兩個下拉與 `input.js` 三個下拉都改呼叫它（後者輸出逐字相同）；契約測試釘住「每個帳戶下拉都走這一個定義、不得自帶市場→幣別表」，e2e 斷言換匯／出金入金的 moomoo_my 選項為「Moomoo MY（USD／MYR）」。 |
| L9 | 再平衡試算 | 預設值＝現權重四捨五入至小數 1 位，合計 **100.10%**，一打開就處於錯誤狀態（此為 F-06 修正的刻意行為，但首次體驗不佳；建議用最大餘額法補回 100.0）。 | **✅ 已修復** — `rebalance.js` 以最大餘額法（Hare）分配預設值：全數有價時剛好 100.0，部分缺價時等於原始權重和的 1 dp 四捨五入（不硬湊）；已儲存的目標配置不動；重設／套用上限也走同一路徑。 | **✅ 驗收完成** — 預設值 `17.7,57.4,2.9,3.0,0.7,0.8,0.7,4.2,0.4,1.0,0.0,0.3,10.9` 合計**正好 100.0**，開啟即「目標合計 100.00%」且無超額警示。 |
| L10 | 再平衡試算 | 目標剛好 100% 時後端回 `cash_after: "-1603.23"`（費稅吃掉現金），畫面「現金水位」被 `Math.max(0, …)` 夾成 0.00%，透支金額完全沒揭露。 | **✅ 已修復** — footer 新增「試算後現金」（後端 `cash_after` 字串，帶正負號）；為負時顯示「⚠ 費稅使現金透支 −1,603.23 TWD — 請下調部分買進」。現金水位保留。 | **✅ 驗收完成** — footer 顯示「試算後現金 **−831 TWD**」並附「⚠ 費稅使現金透支 −831 TWD — 請下調部分買進」；現金水位保留。 |
| L11 | 再平衡試算 | 跨兩個帳戶持有的 AAPL 動作欄為「—」且無說明原因（NVDA 因差額過小同樣為「—」）。 | **✅ 已修復** — 後端 `summary.holds`（`on_target`／`rounds_to_zero`）＋ `summary.excluded_reasons`（`no_price`／`no_rate`／`no_fee_rule`／`no_total`）；前端在「—」旁印出原因（已達目標／差額不足最小單位／缺價／無匯率／無費率），footer 列出被排除的標的與原因。 | **✅ 驗收完成** — 「—」旁印出原因，實測 MSFT／1155／TSLA／NVDA 皆為「差額不足最小單位」；跨兩帳戶的 AAPL 已能產出動作（買 1 股 @ 嘉信 Schwab），不再是無說明的「—」。 |
| L12 | 個股抽屜 · 賣出試算 | 台股（TWSE）可輸入 **0.5 股**並完成試算（手續費取最低 20、稅無條件捨去皆正確），但台股不存在非整數股，缺少整數股驗證。 | **✅ 已修復** — 台股／馬股股數必須是整數：寫入門 `validate_transaction` 新增硬性 `shares_not_integer`（CSV、手動、AI 三個入口共用）；抽屜試算 `strategy/whatif.py` 同規則、`field=shares`。美股刻意不擋（fractional 為 deferred 非禁止，DRIP 本來就會產生小數）。 | **✅ 驗收完成** — 手動輸入台股 0.5 股 → 「✕ 台股股數必須是整數（零股以 1 股為單位），目前是 0.5」且「確認寫入」停用；CSV 匯入同一筆資料也回同一則訊息（共用驗證門確認）。 |
| L13 | 觀察清單 · 快速加入 | 輸入不存在的代號後，系統在 `lookup` 失敗時**自動送出 `POST /api/instruments/ai-resolve`**（付費 LLM 呼叫），使用者並未按下旁邊的「AI 辨識」按鈕。頂欄有「AI 額度 $3.34」的預算概念，打錯字就扣額度不易察覺。 | **✅ 已修復（裁定 3(b)+(d)）** — 原為 R6-B／Wave A1 的刻意設計（lookup miss 自動送一次）。裁定後：純代號型輸入（≤ 6 位英數、無名稱）查無時只提示「查無報價 — 可按 AI 辨識（會使用 AI 額度）」，不自動送；含名稱的輸入照舊自動；設定 → AI 與額度新增「代號查無時自動 AI 辨識」開關（`/api/ui-prefs.auto_ai_resolve`，預設開）。契約測試 5 項。 | **✅ 驗收完成** — 輸入純代號型 `ZZZZ9` 並按「＋ 加入」→ 期間 **`POST /api/instruments/ai-resolve` 呼叫 0 次**（稽核當時會自動送一次），提示改為「查無報價 — 請確認代號與市場；若是用名稱找，可按「AI 辨識」（使用 AI 額度）」，確認／記一筆買入停用；設定 › AI 與額度 有「代號查無時自動 AI 辨識」開關，`/api/ui-prefs.auto_ai_resolve = true`。 |
| L14 | 頂欄新鮮度徽章 | 只有「部分過期」一種過期文案：13 檔全部過期時顯示「部分過期」，只剩 1 檔過期時也顯示「部分過期」。 | **✅ 已修復** — 頂欄徽章依新鮮度面板的清單計數：全部過期顯示「全部過期」，否則「部分過期 n/m」，tooltip 同步。 | **✅ 驗收完成（三態已具備；全部過期文案未觸發）** — 目前 13 檔全新鮮，徽章顯示「**資料新鮮**」（稽核當時只有「部分過期」一種）。原始碼 `app.js:78` 三態齊備：`全部過期` ／ `部分過期 n/m` ／ `資料新鮮`。**「全部過期」需人為製造過期資料才能重現，未在 demo 上製造。** |
| L15 | 儀表板 · 已實現損益卡 | 17 列混合 USD／TWD 顯示原幣金額，**沒有分幣別小計列**，但 API 已提供 `realized.by_currency`（TWD 42,004.56 / USD 389.23）。 | **ℹ 非缺陷（demo 實測，裁定 5(b)）** — 已實現損益卡片頁尾的「各幣別合計」chips（`#realized-footer`，由 `realized.by_currency` 渲染）在 demo 真瀏覽器 390／768／1440 三種寬度都可見（「各幣別合計 USD +97.30 TWD +42,005」）。不改。 | **✅ 驗收完成（確認為非缺陷）** — `#realized-footer` 在 390px 可見且內容為「各幣別合計 **USD +97.30　TWD +42,005**」，與 `realized.by_currency`（USD 97.300573…／TWD 42004.5559…）相符。原稽核判斷有誤（當時只讀了表格元素、沒讀卡片頁尾），此項改判非缺陷成立。 |
| L16 | 交易帳本 · 篩選 | 每次改動日期區間會**同時打 6 個帳本 API**（transactions/dividends/fx/openings/corporate-actions/cash），即使只有一個分頁可見。 | **✅ 已修復** — `ledger.js` 改為只抓**可見分頁**，其餘標記 dirty、切到該分頁時才抓；寫入後的 `pdLedgerRefresh(kind)` 抓可見分頁＋剛寫入的那個分頁（閃爍列仍落在新資料上）。 | **✅ 驗收完成** — 改動日期區間只打 **`/api/ledgers/transactions` 一支**（稽核當時 6 支）；切到「股利」分頁才補抓 `/api/ledgers/dividends`；刪除一筆交易後也只重抓可見分頁。 |
| L17 | 設定 · 排程中心 | 工作摘要輸出未量化的 Decimal：`daily digest 2026-07-22: 組合 -0.06720304659217097625472012746, 警示 0…`。 | **✅ 已修復** — `api/digest_service.py` 工作摘要字串改用推播內文同一個 `_signed_pct` helper：「組合 -0.0672030465921709762547…」→「組合 −6.72%」；儲存的 payload 仍是全精度（測試斷言 > 4 位小數仍在）。 | **✅ 驗收完成（端到端）** — 手動執行 `digest_daily` 產生新列：「daily digest 2026-09-17: 組合 **+2.53%**, 警示 0, 訊號 0; 示範模式略過推播」，頁面上已無 10 位以上的原始 Decimal。*註：2026-07-22 那列歷史字串仍是修復前寫入的舊值，屬既有資料，不影響判定。* |
| L18 | 交易輸入 | 送出／清除後「手續費 20」「交易稅 0」仍留在欄位中，但股數／價格已清空並顯示「股數必須大於 0」。 | **✅ 已修復** — 寫入成功後手續費／交易稅欄位一併清空；`runManualPreview` 把「股數與價格皆空白」視為 pristine（即使代號仍選著），不再印紅字。 | **✅ 驗收完成** — 寫入成功後**股數／價格／手續費／交易稅四欄全部清空**，草稿預覽顯示「—」，且不再出現「股數必須大於 0」紅字。 |
| L19 | 收件匣 | 「刪除並復原」成功後**沒有成功 toast**（「確認入帳」「編輯」「刪除」都有）。 | **ℹ 非缺陷（demo 實測，裁定 5(b)）** — 在 demo 按下「刪除並復原」後，「已復原」toast 於 1.5 秒出現、4.2 秒自動消失，而清單同時在重繪，屬人眼錯過。不改。 | **✅ 驗收完成（確認為非缺陷）** — 逐 0.3 秒取樣：「✓ 已復原 帳本紀錄已刪除，項目將重新出現在收件匣」於 **0.3 秒**出現、約 **4.4 秒**自動消失。原稽核判斷有誤（取樣太晚），此項改判非缺陷成立。驗收後資料逐位元還原（股利 23 筆、收件匣 1 筆、總報酬字串一致）。 |
| L20 | 交易輸入 | 方向切到「買進」時，「當沖（賣出稅 0.15%）」核取方塊仍然顯示且可勾選，對買單無意義。 | **✅ 已修復** — 「當沖」核取方塊比照「放空」：買進時隱藏並取消勾選（`trades.html` 加 `#m-daytrade-line`，`input.js::setSide`）。 | **✅ 驗收完成** — 買進時「當沖」列隱藏且 `checked=false`，切到賣出時顯示，切回買進再次隱藏並取消勾選。 |
| L21 | AI 洞察 · 持倉健診 | 多數卡片顯示「信心 **0.00**」，同時「信心校準曲線」顯示「尚無可比對的卡片」——應區分「信心＝0」與「未提供信心」。 | **✅ 已修復** — 信心改以整數百分比顯示「信心 85%」；信心 0 加 tooltip「模型自評信心 0%（極低）— 非未提供」；`null` 仍是「純描述・無預測」。 | **✅ 驗收完成** — 信心改為整數百分比（實測 80%／70%／65%／60%／50%／40%／0%）；「信心 0%」帶 tooltip「模型自評信心 0%（極低）— 非未提供」；未提供者仍顯示「純描述・無預測」。 |
| L22 | login.html | 目前為訪客模式（任意帳密皆可登入），登入頁完全沒有說明，訪客會誤以為受保護。 | **✅ 已修復** — `login.html` 載入時 GET `/api/auth/session`，`mode=guest` 時顯示提示：「目前為訪客模式：尚未設定授權用戶，任何帳號密碼都能進入，資料未受登入保護。要啟用保護，請在 設定 → 授權用戶 新增用戶。」 | **✅ 驗收完成** — 登入頁顯示：「目前為訪客模式：尚未設定授權用戶，任何帳號密碼都能進入，資料未受登入保護。要啟用保護，請在 設定 → 授權用戶 新增用戶。」 |
| L23 | 儀表板 · 持倉篩選 | 選「台灣券商 ＋ 美股」這種空交集時，表格空白、合計列顯示「0 0」，沒有空狀態說明。 | **✅ 已修復** — 篩選無交集時表格顯示「此篩選條件下沒有持倉 — 所選帳戶與市場沒有交集，請調整上方篩選」，合計列顯示「—」而非 0。 | **✅ 驗收完成** — 選「台灣券商 ＋ 美股」→「此篩選條件下沒有持倉 — 所選帳戶與市場沒有交集，請調整上方篩選」，合計列顯示「—　—」而非 0。 |
| L24 | 更新報價 | 執行成功後 **0056 仍停在 2026-09-11**（其餘 12 檔更新為 09-14／09-15），介面沒有任何逐檔失敗說明。 | **✅ 已修復** — 報價工作結果新增 `lagging`（同一批成功抓取、但最新收盤日仍落後同市場最新日的持倉）；更新報價完成的 toast 會點名「N 檔資料來源未提供更新（仍為舊日期）：0056」，並另出一則 warn。狀態仍為 ok（資料確實是來源給的）。 | **✅ 驗收完成（機制已具備；本次無落後標的可觸發文案）** — `POST /api/actions/refresh-quotes` 回應三個 job 各帶 `lagging: []`。本次 1155／5225 同為馬股最新收盤日 2026-09-15，依「落後同市場最新日」的定義不算落後，因此文案未觸發 —— 屬定義內的正確行為，非漏改。 |
| L25 | 更新報價 | 完成後**整頁重新載入**，已展開的卡片、篩選條件、捲動位置全部流失。 | **✅ 已修復** — 重新整理保留：`shell.js` 在 reload 前呼叫 `pdBeforeReload`，`app.js` 把持倉篩選／排序、展開中的 `<details>`、捲動位置寫進 sessionStorage，開機時讀回一次。仍採整頁重載（圖表／摘要／鈴鐺各自讀自己的端點，局部重繪會讓同一畫面出現兩個版本的資料）。 | **✅ 驗收完成** — 更新報價後整頁重載，**帳戶 chip（台灣券商）、展開中的「資料新鮮度」、捲動位置（1200 → 1227）全部還原**。 |
| L26 | 觀察清單／產業配置 | `0056 元大高股息`（ETF）產業被歸為 **Financials**，直接影響儀表板「產業配置」圓餅（279,250 TWD 計入金融）。設定中已有「ETF」分類可用。 | **✅ 已修復（demo 資料）** — demo DB 先備份（`portfolio-pre-audit-fixes-20260916T000317Z.db`，integrity ok）再 `UPDATE instruments SET sector='ETF' WHERE symbol='0056'`；`scripts/seed_demo.py` 本來就寫 `sector="ETF"`，是 demo 資料在後續操作中被改成 Financials。走 `vm_exec.py`，有記錄。 | **✅ 驗收完成** — 0056 列的產業為 **ETF**；`/api/dashboard` 的 `allocation.by_sector` 出現獨立的 `ETF: 277,750`，`Financials` 由 528,692 降為 247,742。 |
| L27 | 種子資料品質 | `dividend_events` 最早記錄 **1987-05-11**；已實現損益有 4 筆完全相同的 TSLA（5 股／1,299.96 USD／+49.96）與 4 筆近乎相同的 MSFT；2330 有兩筆 2026-07-03、各 3,000 的現金股利；5225 在 2026-06-07 有 4 筆各 +200.00 MYR。可能為 seed 腳本重複寫入。 | **✅ 已修復（demo 資料，裁定 2(a)）／ℹ 1987 年列非缺陷** — 查證結果（demo DB 唯讀）：`dividend_events` 1987-05-11 是 **AAPL 在 yfinance 的真實配息史**（1987 年起有派息），非種子重複；TSLA 4 筆與 MSFT 4 筆賣出（id 20/38/49/59、23/41/52/62）第一筆是原始種子（舊費率快照），後三筆是 fee-engine v2 之後由測試流程重複寫入的同內容列；2330 於 2026-07-03 兩筆 3,000（id 3、4）、5225 於 2026-06-07 四筆 200（id 11/19/28/31）同理。這是累積的壓測語料，不是 seed 腳本重複執行。裁定 2(a)：重複列已刪除（先備份 `portfolio-pre-dedupe-20260916T105900Z.db`，走 `vm_exec.py`）：交易 62→56、股利 27→23；dashboard 前後比對只有 TSLA／MSFT／2330／5225 四檔與已實現損益（17→11 列、USD 389.23→97.30）變動。1987 年的 AAPL 派息史不動。 | **✅ 驗收完成** — 交易 **56** 筆、股利 **23** 筆、已實現損益 **11** 列、`realized.by_currency.USD` = 97.30（稽核時 62／27／17／389.23）；重複列已不存在。1987-05-11 的 AAPL 派息史依裁定保留。 |

---

## 三、驗證通過的項目（重要：這些都測了，而且是對的）

### 金額與成本計算 — 93 項恆等式全數通過

以 `/api/dashboard` 原始 Decimal 字串獨立重算：

- `已實現 + 未實現 = 資產損益` ✅
- `含匯兌總損益 = 資產損益 + 本金匯率效果 + 交易與融資成本` ✅
- `Σ 各帳戶市值 = Σ 各市場市值 = 總市值` ✅
- `Σ 各幣別（報告幣）= 總市值`、`Σ 產業配置 = 總市值`、`Σ 產業權重 = 1`、`Σ 逐檔權重 = 1` ✅
- 逐檔（14 檔 × 6 項）：`市值 = 股數 × 現價`、`未實現 = 市值 − 調整成本`、`資本利得 = 市值 − 原始成本`、`未實現 = 資本利得 + 股利沖減`、`調整成本 = 原始成本 − 股利沖減`、`未實現% = 未實現 ÷ |原始成本|` ✅

> 補充：「未實現損益%」以**原始成本**為分母（分子含股利），乍看基準不一致，但 `portfolio/dashboard.py:688-698` 有完整註解說明這是 audit H1 的刻意決定（調整成本可能 ≤ 0 會翻轉正負號，放空部位也會翻轉），且抽屜內明確標示「vs 原始成本」。**判定為正確，非缺陷**。

### 費稅規則（逐筆手算比對）

| 情境 | 系統值 | 手算 |
| --- | --- | --- |
| 台股賣出 2,600 @ 2,470 | 手續費 9,151／證交稅 19,266／淨收 6,393,583 | 6,422,000×0.1425%=9,151.35→9,151；×0.3%=19,266 ✅ |
| 台股買進 1,000 @ 2,500 | 手續費 3,562／稅 0／總成本 2,503,562 | 2,500,000×0.1425%=3,562.5→3,562 ✅ |
| 當沖賣出 100 @ 45.55 | 稅 6 | 4,555×0.15%=6.83→6 ✅ |
| 零股賣出 0.5 @ 2,470 | 手續費 20（取最低）／稅 3 | 1,235×0.1425%=1.76→取 min 20；×0.3%=3.7→3 ✅ |
| 賣超 5,000／持有 2,600 | 明確警告＋公司行動補登入口＋成本基礎歸零確認框 | ✅ |

### 寫入／刪除／重建 — 三條路徑逐位元還原

| 路徑 | 寫入驗證 | 還原驗證 |
| --- | --- | --- |
| 交易（新增 → 編輯 → 刪除） | `#67` 建立後市值 +4,555、總報酬 **−20**（＝手續費）、2884 股數 100→200、均價 93.2→69.475；編輯成 150 股後 →250 股／16,172.50 成本 | 刪除後 KPI／持倉／筆數與基準**字串完全相同** |
| 股利（收件匣確認入帳 → 復原） | 調整成本 −133,630、股利沖減 +133,630、調整均價 −41.50（＝133,630÷3,220）、未實現 +133,630、總市值 **不變**、回本率 4.87%→11.67% | 復原後全部還原，項目自動回到收件匣 |
| 換匯（寫入 → 帳本刪除） | MYR −4,000／USD +983.28、取得均價 4.384338→4.379134、**總報酬完全不變**（符合不變量 6：換匯不產生損益） | 現金池、均價、`reporting_total` 全部逐位元還原 |

### 其他通過項目

- **現金收支滾動餘額**：Moomoo MY／MYR 全 31 列逐列驗算 `前一列餘額 + 本列金額 = 本列餘額`，全數相符，期末 509,066.42 與 `/api/cash` 一致。
- **匯出中心**：15 個端點（CSV／ZIP／HTML／JSONL）全部 200，Content-Disposition、UTF-8 檔名、內容皆正確（含年度稅務包 ZIP、全帳本 ZIP、可列印 HTML 報告）。
- **持倉篩選**：帳戶 × 市場 8 個 chip 與合計列，逐一與 `holdings_subtotals` 比對全數吻合。
- **排序**：12 欄可排序欄位升冪／降冪皆正確（含跨幣別分組排序）。
- **趨勢圖／績效比較**：1M/3M/6M/全部 以 `dataZoom` 正確裁切（254 點 → 222–253 / 161–253 / 69–253 / 0–253）；市值 ↔ 績效比較切換正確；0050 ↔ S&P 500 基準切換正確取得不同資料（終值 156.108 vs 109.963）。
- **日期區間篩選**：套用後 13 筆全部落在區間內；**顛倒區間後端回 400 並顯示「日期區間無效」**；未來區間回 200／0 筆；清除後還原 62 筆。
- **CSV 匯入預覽**：合法 CSV 逐列「✓ 可寫入」；非法 CSV 給出精準訊息（「欄位 price 的內容『abc』不是有效的數字（請移除千分位逗號、貨幣符號與空白）」、「必填欄位不可空白（欄位 price）」）。
- **搜尋（⌘K）**：代號／中文名／英文名模糊比對、標示持倉／觀察、查無時導引到觀察清單、方向鍵＋Enter 開抽屜，全數正常。
- **刪除防護**：交易／股利／換匯刪除皆有確認彈窗，「取消」不發任何請求。
- **通知中心 demo 防護**：62 個控制項中 48 個 disabled，並顯示「示範站不開放通知設定」。
- **乾跑預檢**：R1–R8 逐道檢核、零成本、可預覽組裝後提示詞。
- **RWD**：390×844 下 index／settings 皆 `scrollWidth === 390`、零非法溢位；表格以 `overflow-x:auto` 正常內捲；手機側欄開合正常。
- **轉址頁**：`input.html` / `ledger.html` 正確轉址至 `trades.html`。

---

## 四、未執行的項目與原因

| 項目 | 原因 |
| --- | --- |
| AI 洞察「產生洞察」、個股「重新產生」、新聞「抓取新聞」、「AI 輸入」解析 | 會實際消耗 LLM 額度（站上餘額 $3.34），未經你同意不主動花費 |
| 設定 → 授權用戶「新增用戶」 | 會立即啟用帳密保護，可能把 demo 站鎖住 |
| 各處「⬇ 匯出 CSV／⎙ 匯出報告」按鈕的實際下載 | 改為直接呼叫對應後端端點驗證（15/15 通過），避免往你電腦寫入檔案 |
| 登入頁實際送出帳密 | 不代為輸入密碼類欄位 |
| 資料來源「測試」連線（Alpha Vantage／Finnhub／Schwab API） | 未設定金鑰；TWSE／twstock／Bursa 官網本來就標示連線失敗或受阻 |

---

## 五、測試期間造成的站台狀態變更（請知悉）

1. **已執行一次「更新報價」**（頂欄 ⟳ → 更新報價）。結果：報價由 09-07/09-08 更新為 **09-14（台股／馬股）與 09-15（美股）**，三組匯率更新為 **09-15**。因此站上數字已與測試開始時不同：
   - 總市值 `35,795,482.86` → `33,724,612.69`
   - 資產損益 `24,845,621.63` → `22,742,982.87`
   - XIRR `12.4918` → `11.0261`
   - 鈴鐺警示 34 → 24（過期報價警示減少）
   - 各幣別**原幣**市值（TWD 32,089,490／USD 102,282.9759…／MYR 62,980）**完全未變**，證明帳本沒有任何殘留污染。
2. 測試中新增的 1 筆交易（#67）、1 筆股利、1 筆換匯（fx #20）**均已刪除**，所有金額逐位元還原至基準值。
3. 2412 中華電信的目標價曾暫時設為 100/150，已清空還原。

---

## 六、建議的修復優先序

1. **H1 再平衡 float 洩漏**（前後端各補一行，風險最低、最像 bug）
2. **M4 寫入後持倉提示錯帳戶**（會誤導連續輸入）
3. **M2 管線輸入節點 scope 判斷**（單一函式重排三行）
4. **M5 帳戶名稱雙軌**（把 3 支檔案接上 `pdNames`，並移出 `_PENDING`）
5. **M3 排程器狀態揭露 ＋ digest 過期標示**
6. **M1 匯率三角一致性**（設計決策，建議先做檢核與揭露，再談改資料源）
7. **M6 / M7 / M8** 三項互動語意修正
8. **M9 AI 卡數值後置檢核**（現有「AI 戰績」已提供衡量基準）
9. L 系列擇要批次處理（L4／L5 文案、L1 排序說明、L14 徽章文案成本最低）

---

_報告產生於 2026-09-16 01:14（Asia/Taipei）・受測版本 v0.1.28 · 85dfafd_

---

## 七、修復後待你決策的問題（2026-09-16 補記）

編號對應上方各項目「修復狀態」中的「問題 N」。每題都附建議，回覆選項即可。

> **2026-09-16 已全數裁定並執行，結果見 §八；本節保留為裁定前原文。**

1. **M1 匯率三角一致性 — 要不要改資料源？** 目前只做檢核＋揭露（面板會顯示落差）。若要根治，需指定一個 base（建議 **USD**），把 MYR/TWD 改為 `USD/TWD ÷ USD/MYR` 推導後儲存（`source` 記為 derived），三組永遠閉合；代價是報告幣別的馬股數字會從現值微調約 0.05–0.13%，且是「資料來源」層級的決策。另外 `0.05%` 的落差門檻是實作選擇，可調。
2. **L27 demo 重複列 — 刪不刪？** 查證為累積的重複寫入而非種子腳本重複執行：TSLA 賣出 id 38／49／59 與 MSFT 41／52／62（與 id 20／23 完全相同，只差費率快照版本）、2330 股利 id 4（與 id 3 相同）、5225 股利 id 19／28／31（與 id 11 相同）。建議 **刪除**（它們不是壓測情境，是同一情境寫了四次），先備份、走 `vm_exec.py`；副作用是 demo 的已實現損益、回本率、TSLA／MSFT 持股數會變。1987 年的 `dividend_events` 是 AAPL 真實派息史，不動。
3. **L13 觀察清單自動 AI 辨識 — 三選一。** (a) 維持現狀（R6-B 設計：lookup miss 自動送一次）；(b) **建議**：純代號型輸入（≤ 6 位英數、無中文）miss 時不自動送，只顯示「查無報價 — 可按 AI 辨識（會使用 AI 額度）」，含中文或名稱的輸入才自動；(c) 全改手動按鈕。
4. **M3 demo 排程器 — 維持關閉？** 現在畫面會明說「排程器未啟動」。建議 **維持關閉**（demo 資料可重現是驗證流程的前提）；摘要過期門檻（今日摘要 > 2 天、週行動清單 > 8 天）為實作選擇，可調。
5. **L15／L19 無法重現 — 請補一個線索。** L15：已實現損益卡片頁尾在程式中有「各幣別合計」chips，請告訴我你看不到時的視窗寬度或截圖。L19：程式路徑上「刪除並復原」成功後有「已復原」toast，請告訴我按的是哪個按鈕（「本次已入帳」條上的「復原」？還是別處），或允許我在 demo 上重現一次（會刪一筆股利再由收件匣入回）。
6. **這批要不要 commit＋push（不併 main）＋部署 demo？** 依慣例等你指令；prod 不動。
7. **M9 數值後置檢核的參數** — 每卡最多列 5 筆、相對容差 0.5%、`_NOT_A_TICKER` 排除名單（PE／ETF／USD／Q1…）都是實作選擇；接受預設即可，或指定數值。

## 八、七題裁定與執行結果（2026-09-16 補記）

| 題 | 裁定 | 執行結果 |
| --- | --- | --- |
| 1 M1 | (b) base = USD，MYR/TWD 改推導 | 已實作：`pricing/cross.py` 在每次 FX 寫入後推導 `MYR/TWD = USD/TWD ÷ USD/MYR`（最新報價與歷史回補都經過、涵蓋整段歷史），`source=derived:USD`；抓取清單移除 MYR/TWD；新鮮度面板標「推導」；三角檢核從此應讀 0。規則記入 `rules/data-and-pricing.md`。demo 要按一次「更新報價」才會把既有的 provider 列換成推導列（已於部署後執行：1,346 列改為 `derived:USD`，三角落差 0.0000%）。 |
| 2 L27 | (a) 刪除重複列 | 已執行（先備份 `portfolio-pre-dedupe-20260916T105900Z.db`）：交易 62→56、股利 27→23；dashboard 前後比對只有 TSLA／MSFT／2330／5225 四檔與已實現損益（17→11 列、USD 389.23→97.30）變動。 |
| 3 L13 | (b)+(d) | 已實作：純代號型輸入（≤ 6 位英數、無名稱）查無時只提示不自動送；名稱型照舊自動；設定 → AI 與額度新增「代號查無時自動 AI 辨識」開關（`/api/ui-prefs.auto_ai_resolve`）。 |
| 4 M3 | (a) 維持關閉、門檻 2／8 | 不動。 |
| 5 L15／L19 | (b) 在 demo 重現 | 已用真瀏覽器實測：L15 頁尾「各幣別合計 USD +97.30 TWD +42,005」在 390／768／1440 都可見（非缺陷）；L19 的「已復原」toast 在按下「刪除並復原」後 1.5 秒出現、4.2 秒自動消失，而清單同時在重繪，屬人眼錯過（非缺陷；不改）。 |
| 6 | 先裁定本機 commit、不 push；同日稍後改為 commit＋push（不併 main）＋部署 demo | `909f0b2`（31 項修復）、`486bbb8`（七題裁定）、`7445208`（部署後在 demo 發現的最後一處英文帳戶名：`cash.js`／`input.js` 讀 context 清單的 `a.name`，加靜態守門）已 push 至 `feat/corporate-actions`；`origin/main` 未動（`734833b`）。demo 依序部署 `486bbb8` → `7445208`（各自先備份 `portfolio-pre-486bbb8-20260916T152404Z.db`、`portfolio-pre-cashnames-20260916T153507Z.db`），`verify_live` ALL PASS，真瀏覽器走查 0 錯誤、390px 無溢位；prod 未動。 |
| 7 M9 | 接受預設 | 不動。 |

截至 2026-09-17，本報告無待決事項。

---

## 九、複驗（驗收）報告 — 2026-09-17

| 項目 | 內容 |
| --- | --- |
| 複驗對象 | demo 站 `v0.1.28 · **7445208**`（`/api/health`），與報告 §八 所述部署版本一致 |
| 複驗時間 | 2026-09-17 01:5x – 02:4x（Asia/Taipei） |
| 複驗方法 | 由原稽核者操作真實瀏覽器，**逐項重放原報告的「重現步驟」**；輔以 `/api/*` 回應逐位元比對、以及對 `feat/corporate-actions` 工作區原始碼的交叉確認 |
| 判定原則 | 「修復狀態」寫了什麼，就驗什麼；沒能在畫面上重現的情境，明確標示為「原始碼確認」而非「已驗證」 |

### 9.1 計分

| 判定 | 數量 | 項目 |
| --- | --- | --- |
| ✅ 驗收完成 | **32** | H1、M1、M2、M3、M4、M5、M6、M7、M8、L1、L2、L3、L4、L6、L7、L9、L10、L11、L12、L13、L14、L16、L17、L18、L20、L21、L22、L23、L24、L25、L26、L27 |
| ✅ 確認為非缺陷 | **2** | L15、L19（原稽核判斷有誤，已在該列更正） |
| ⚠️ 部分通過 | **2** | L5、L8 |
| ❌ 未通過 | **1** | M9 |

### 9.2 回歸測試（確認修復沒有打壞原本就對的東西）

| 檢查 | 稽核當時 | 複驗 |
| --- | --- | --- |
| 金額恆等式 | 93 項全過 | **94 項全過**（10 組總表恆等式 ＋ 14 檔 × 6 項逐檔；新增「FX 三角落差 < 0.001%」一項，實測 0.000001%） |
| 匯出端點 | 15/15 → 200 | **15/15 → 200**，Content-Disposition 與內容型別皆正確 |
| 寫入／刪除還原 | 3 條路徑逐位元還原 | **交易（#68）、股利（收件匣確認→復原）兩條路徑複驗，皆逐位元還原** |
| 390px 橫向溢位 | 0 處 | **0 處**（index／insights／pipeline-hub／settings，settings 的 8 個分頁逐一檢查，`scrollWidth` 恆為 390） |
| 正常操作 console error | 0 | **0**（唯一的 400 來自我自己故意送出的顛倒日期區間測試） |

### 9.3 未通過項目

#### ❌ M9 — AI 洞察卡數值後置檢核

**(a) `unverified_figures` 從未觸發，稽核點名的那張卡仍沒有徽章。**

| 觀察 | 值 |
| --- | --- |
| 卡片總數 | 148 |
| `figure_flags` 欄位存在 | 148 / 148 ✅ |
| **有 `unverified_figures` 的卡片** | **0** |
| 卡片 #37「美股部位：科技雙巨頭領漲，未實現收益達 **429 萬美元**」（1000 倍尺度錯誤，稽核原文的頭號例子） | `unverified_figures: []`，畫面上**沒有**「數值待核」徽章 |
| 卡片 #40「未實現收益 **4,290.80 美元**」（同批正確值） | 同樣 `[]`（正確，不該被標） |

**根因（原始碼）**：`llm_insight/figure_check.py` 的 `_unverified_figures()` 在快照沒有可解析數字時直接回空 —— 模組 docstring 明寫「a snapshot with NO parseable number at all yields NO figure flags」。2026-07-05 產生的舊卡沒有可用的 `input_snapshot`，檢核因此**靜默略過**。修復註記寫的「舊卡片因快照仍在，讀取時同樣會被標」與站上實測不符：單元測試用的是合成快照，真實舊卡沒有。

**(b) `unknown_symbols` 誤報率約 98%。**

148 張卡中 27 張被標，48 個代號實例的分佈：

| 代號 | 次數 | 是不是真的幻覺代號？ |
| --- | --- | --- |
| **6883** | 1 | ✅ 是（稽核點名的 LRDIM） |
| PBR | 12 | ❌ 股價淨值比 |
| MA20 | 8 | ❌ 移動平均 |
| MA60 | 8 | ❌ 移動平均 |
| MA120 | 6 | ❌ 移動平均 |
| MA200 | 5 | ❌ 移動平均 |
| MA50 | 4 | ❌ 移動平均 |
| RSI14 / BUY / HOLD / KLCI / TAIEX | 各 1 | ❌ 指標／評級／指數名 |

**根因（原始碼）**：`figure_check.py:81` 的 `_NOT_A_TICKER` 只收錄**裸形**（`"MA"`、`"RSI"`、`"PE"`、`"PB"`…），沒有帶週期的變體（`MA20/MA50/MA60/MA120/MA200`、`RSI14`），也沒有 `PBR`／`KLCI`／`TAIEX`／`BUY`／`HOLD`。

**(c) 附帶：徽章文案與實際命中不符。** 只命中 `unknown_symbols` 時，徽章仍叫「數值待核」、tooltip 仍寫「卡片中的數字未出現在餵入的變數快照中，請以儀表板數字為準」—— 實測 tooltip 為「未知代碼：MA20、MA60、MA120\n卡片中的數字未出現在餵入的變數快照中…」，兩句互相矛盾。

**淨效果**：這道檢核目前**抓不到它要抓的東西**（尺度錯誤），卻在 27 張正常卡片上掛了紅字。以使用者體驗論，比修復前更差 —— 徽章一旦常態誤報就會被忽略，真正的 6883 也跟著被忽略。

**建議修法**
1. `_NOT_A_TICKER` 從「名單」改成「名單＋規則」：`^(MA|EMA|SMA|RSI|KD|ATR|MACD|BOLL)\d*$`，並補 `PBR`／`PSR`／`PEG`／`KLCI`／`TAIEX`／`BUY`／`HOLD`／`SELL`。
2. 快照缺漏時**不要靜默略過**：標成「無快照可核」（第三種狀態），否則舊卡永遠不會被檢查，而舊卡正是問題最多的一批。
3. 徽章與 tooltip 依命中類型分文案：只命中代號時用「未知代碼」，只命中數字時用「數值待核」。
4. 建議補一個以**真實舊卡**（#37）為輸入的回歸測試，而不是只用合成快照。

> **修復回覆（2026-09-17）**：四點建議全部採納並實作；根因更正為「149/149 張卡從未存過變數快照，`RunInputs.input_snapshots` 無人餵入」，母體改為實際提示詞內的數字並存進新欄。詳 §十.1。

### 9.4 部分通過項目

#### ⚠️ L5 — 中文標點半形／全形混用

稽核原文**點名的三句都已修**：

| 原文 | 現況 |
| --- | --- |
| `配息／配股偵測,台美馬全市場` | ✅ `配息／配股偵測，…` |
| `排程:每日 06:00(設定→排程 可調)` | ✅ `排程：每日…` |
| `偵測是持續進行的:每日排程掃描,…` | ✅ `偵測是持續進行的：每日排程掃描，…；…` |

但**同一類問題仍在**（以下皆為 rendered text，非程式註解）：

| 位置 | 實測字串 | 數量 |
| --- | --- | --- |
| 收件匣 › 待確認退款區塊 | 「台股券商先收後退**:**次月退回…記入該帳戶現金池**,**不計入成本／損益／XIRR」、「台股先收後退**:**預估僅供參考**,**確認後…現金池**,**不計入成本／損益。」（3 張卡各一組） | **16 處** |
| 新聞庫 | 「逐則 token/成本追蹤**,**協助評估用量」、「全部**(**持倉＋觀察清單**)**」 | 2 處 |
| 再平衡試算 footer | 「（台股先收後退**,**不計入成本）」（`web/rebalance.js`） | 1 處 |
| 資金管理 | 「合併現金（TWD，依最新匯率換算）**:**」 | 1 處 |
| `web/news.js` | 「**(**此則僅取得標題，未整理內文摘要**)**」 | 1 處 |
| `web/ledger.js` 確認框 | 「交易日期 2099-12-31 晚於今日**,**確認無誤**?**」 | 1 處 |

**建議**：加一條靜態測試 —— 掃 `web/*.html` 的可見文字節點與 `web/*.js` 的中文字串常量，禁止「CJK 字元相鄰的半形 `, : ; ! ? ( )`」（排除程式註解與英文句）。一次掃乾淨，並防止回流。這是典型的「修了實例、沒修類別」。

> **修復回覆（2026-09-17）**：建議的靜態測試已加入並以稽核原句作陽性對照；前端 25 處＋點名的後端 2 處全改。後端另有 96 處提為 Q1，同日裁定分批並兩批掃完（§十.5）。詳 §十.2。

#### ⚠️ L8 — 帳戶下拉的幣別標註

| 位置 | 現況 |
| --- | --- |
| 交易帳本 › 手動交易 `#m-account` | ✅ `Moomoo MY（USD／MYR）`、`嘉信 Schwab（USD）`、`台灣券商（TWD）` |
| 交易帳本 › 股利 `#d-account` | ✅ 同上 |
| 交易帳本 › 期初庫存 `#o-account` | ✅ 同上 |
| **資金管理 › 換匯中心 `#cfx-account`** | ❌ 仍為 `Moomoo MY（**USD**）` |
| **資金管理 › 出金入金 `#cm-account`** | ❌ 仍為 `Moomoo MY（**USD**）` |

稽核原文的標題就是「交易輸入／**股利**／**換匯** 帳戶下拉」，換匯那一個沒跟上。中文帳戶名（M5）在這兩處是對的，只有幣別後綴沒同步。

> **修復回覆（2026-09-17）**：推導邏輯改為 `names.js` 單一定義，兩個下拉已同步，契約＋e2e 釘住。詳 §十.3。

### 9.5 三項以「原始碼確認」結案的說明（並非省略，而是站上目前無法製造該情境）

複驗當下 demo 的 13 檔報價**全部新鮮**（2026-09-15／16），以下情境因此無法在畫面上重現。我沒有為了驗收去竄改 demo 的報價資料，改以原始碼確認：

| 項目 | 需要的情境 | 原始碼確認 |
| --- | --- | --- |
| M2 的 AAPL 去重 | 副標只在「有標的缺價／過期」時出現 | `insight_service.py::_price_population` 以 `dict.fromkeys` 去重保序 |
| M8 的對立情境 | 需有標的過期，才能比較兩個介面 | `_price_state_for()` 為單一 helper，`_gather_facts`／`_preflight_saved`／`_preflight_draft` 三處共用 |
| L14 的「全部過期」 | 需 13 檔全部過期 | `app.js:78` 三態齊備：`全部過期` ／ `部分過期 n/m` ／ `資料新鮮` |

若要把這三項也做成畫面級驗證，可在 demo 上建一個「把報價日期往前推 N 天」的測試開關（或用一份凍結的備份 DB 跑一次），我可以再驗一輪。

> **回覆（2026-09-17）**：不建議在 demo 加「報價日期往前推」開關——demo 的合成帳本是累積中的壓測語料（owner 裁定 2026-07-31 不重置），而一個能改寫報價日期的開關會成為 prod 面向的表面。三個情境已由本機隔離測試以老化夾具覆蓋：M2 `tests/contract/test_pipeline_hub_api.py::test_portfolio_task_input_head_is_all_holdings_and_dedupes`／`test_per_market_task_input_head_counts_markets`，M8 同檔 `test_card_and_dry_run_agree_on_stale_price`／`test_card_and_dry_run_agree_on_missing_price`，L14 `tests/e2e/test_flows_e1_e10.py`（六月老化夾具，chip 文案「全部過期」）。維持「原始碼確認＋隔離測試」結案。

### 9.6 複驗期間造成的站台狀態變更（請知悉）

1. **手動執行了一次 `digest_daily`**（設定 › 排程中心 › 立即執行）—— 這是 L17 端到端驗證所需。副作用：儀表板「今日摘要」由 2026-07-22 的內容更新為 **2026-09-17**，M3 的「已過期・56 天前產生」標記因此不再顯示（該標記已於更新前擷取並記錄於 M3 驗收欄）。舊卡片仍可由「歷史」按鈕查看。
2. **按了兩次「更新報價」**（其中一次是 L24／L25 驗證）—— 報價更新至 2026-09-16、匯率至 2026-09-16。
3. 測試寫入的 **1 筆交易（#68）** 與 **2 次股利確認／復原** 皆已刪除還原，所有 KPI 與筆數回到複驗前的值。
4. 期間送出過數次故意的無效請求（顛倒日期區間 → 400、不存在的代號 → lookup 查無），僅產生 `job_runs`／`action_log` 記錄，未改動帳本。

### 9.7 結論

這批修復的**工程品質高**：H1 的根因在前後端兩側都堵住了、M1 從「只做檢核」升級成「資料源推導」讓三角落差從 0.0690% 降到 0.000001%、M2／M8 都把重複的判斷抽成單一 helper、M6 的 history 行為六個情境全通過、L16 把 6 支 API 降成 1 支。回歸也乾淨，沒有任何一項原本通過的檢查退化。

需要再處理的只有三項：**M9 要重做**（目前抓不到該抓的、卻誤報 27 張卡），**L5／L8 要把同類掃乾淨**（現在是修了實例、沒修類別）。其餘 34 項驗收完成。

> **修復回覆（2026-09-17）**：三項皆於同日處理完畢（M9 重做、L5／L8 改為類別修法＋守門），並延伸掃完後端 96 處中的 86 處可見文字（Q1 兩批），見 §十；已 commit 到分支（`cbfac0a`、`55e0216`），待部署後請再複驗一輪。

## 十、複驗回覆與修復（2026-09-17，修復作業補記）

> 三項複驗意見逐一比對原始碼與 demo 資料庫後**全部成立**。修法原則：**修類別、不動已驗收項目**——不改 `input_snapshot`／快取指紋／Loop-2 評分輸入，交易帳本三個已驗收下拉的輸出逐字不變，後端 96 處字串提為 Q1（同日裁定並兩批完成）。閘門：ruff clean（`portfolio_dash`／`tests`／`scripts`）・`mypy --strict --no-incremental` 820 檔，在閘門範圍（`portfolio_dash`／`tests`／`scripts/probe`）0 錯誤（285 個錯誤全在範圍外的 `scripts/stress_audit`，既有）・pytest 非 e2e 全套 **4,958 通過／4 略過／0 失敗**・e2e：`test_cash_withdraw_estimate_flow.py`＋`test_pages_smoke.py`＋`test_corporate_actions_flow.py` 52 通過／22 略過／1 失敗——該失敗為 `909f0b2`（M5 把預覽卡帳戶名改成中文）遺留的舊測試期望值 `"Charles Schwab"`，與本次改動無關，已改為「嘉信 Schwab」後單獨重跑通過。本輪依裁定 commit 到分支（見 §十.7），未 push、未部署 demo。

### 10.1 M9 — 根因、修法與邏輯

| 面向 | 複驗所述 | 實際根因（本次查證） | 修法 |
| --- | --- | --- | --- |
| (a) 母體 | 2026-07-05 的舊卡沒有可用 `input_snapshot` | **所有**卡都沒有：demo 唯讀查詢 149/149 列（最新 2026-08-25）的 `input_snapshot` 長度 13～20 字元，內容 `YYYY-MM-DD|標的`——`generate.py::_snapshot_for()` 的 fallback；`RunInputs.input_snapshots` 在整個套件裡沒有任何呼叫者餵入。修復註記「舊卡片因快照仍在」錯誤 | 產生端在呼叫模型的同一處，用 `figure_check.prompt_figures_json(prompt)` 把**實際提示詞**內的數字（去重、保留 `%` 形式、萬／億／K 先換算、年份與日期一併保留以免母體過小）存成 JSON 清單到**新欄** `insights.prompt_figures`；讀取端 `_figure_flags` 以 `prompt_figures or input_snapshot` 為母體 |
| (a′) 第三態 | 快照缺漏時不要靜默略過 | 舊版母體為空即回 `[]`，「無法核對」被讀成「乾淨」 | `FigureFlags.snapshot ∈ {checked, none}`；`none` 的定義是「卡片印了至少一個數字、且母體無任何數字」。純敘事卡不掛徽章；零 LLM 的資料異常卡標題 `2330 資料異常` 因「已註冊代號裸寫不算數字」為 `checked` |
| (b) 代碼 | `_NOT_A_TICKER` 只收裸形 | 名單無法列舉帶週期的家族 | `_INDICATOR_RE` 形狀規則＋名單分組擴充（市場／幣別、比率、總經、指標、指數、評等）；先查註冊表再查名單。`_CODE_RE` 由 `\d{4}` 放寬到 `\d{4,6}` |
| (c) 徽章 | 只命中代碼仍叫「數值待核」 | 一種徽章配一句 tooltip | `insights.html::figureFlagPill` 三種；`.badge-unchecked` 灰色 |

**為什麼不直接把變數快照塞進 `input_snapshot`：** 它的內容進快取指紋（`snapshot_digest`）——變數 JSON 含秒級時間戳時，同日重跑永遠 miss，會重現 2026-07 修過的「每次重跑都計費＋重複卡」；它也是 master 評分提示詞的 `<input_snapshot_at_creation>`，每次評分 token 會變多。額外欄位把影響面限制在讀取時的徽章。

**對 demo 的可見影響（部署後）：** 現有 149 張卡中有印數字的會掛灰色「無快照可核」，沒印數字的不掛；「未知代碼」只剩真正的 6883 那張；之後新產生的卡才會出現「數值待核」。5 筆上限與 0.5% 容差不動（裁定 7）。

**測試（新增 8、更新 3）：** `test_figure_check.py`（#37 真實儲存形態→`none`；`prompt_figures_json` 母體；指標／比率／評等／指數不是代碼；5～6 位代碼不是數字）、`test_generate.py`（真實產生路徑＋stub 模型，在儲存列上斷言；資料異常卡無母體且 `checked`）、`test_insights_store.py`（舊表加欄、往返、預設 `''`）、`test_insight_run_api.py`（wire 多 `snapshot` 鍵；舊列在 `/api/insights` 回 `none`；頁面三種徽章文案）。

### 10.2 L5 — 類別掃描

- 掃描器（與新測試同一邏輯）在 `web/` 找到 **25 處字串／11 檔**（複驗的「16 處」是渲染後實例數，對應 `rebate-inbox.js` 4 句＋`dividend-inbox.html` 4 句）：`dividend-inbox.html`、`news.html`、`insights.html`、`app.js`、`cash.js`、`corp-action-form.js`、`input.js`（CSV 欄位提示 5 句，同句內既有 `（選填）` 故整句統一全形）、`news.js`、`rebalance.js`、`rebate-inbox.js`。全數改為 ，：；？（）～。
- 複驗點名的「`ledger.js` 確認框」那句來自後端 `data_ingestion/validate.py`（`晚於今日,確認無誤?`），與 `改記為分割(SPLIT)` 按鈕標籤（`api/routers/ledgers.py`；前端只是 fallback）一併改，對應 e2e 期望值同步。
- 新守門 `tests/contract/test_zh_punctuation_fullwidth.py`：HTML 文字節點＋`title/placeholder/aria-label/alt/value` 屬性＋`<script>` 內字串；JS 用狀態機取字串／模板常量並跳過註解（regex 分不出網址裡的 `//` 與註解）；規則「半形 `, : ; ! ? ( )` 任一側緊鄰 CJK」；含陽性對照（稽核原句必須被抓到）與空白 allowlist 的過期檢查（D39）。
- **未納入：** 後端 `portfolio_dash/**` 字串常量（AST 掃描、排除 docstring）另有 **98 處**（改 2 處後 **96**；先前寫 121 是把 25 個前端行也算進去了，此處更正）——其中 **57** 處是使用者可見訊息（驗證訊息、確認框、toast、匯出標籤）、**29** 處是「更新紀錄」目錄（`shared/whatsnew.py`）、**10** 處是 regex 與 LLM 提示詞（不需處理）。實測只有 **6** 處被測試逐字釘住、8 處出現在文件／頁面；未納入的理由不是測試，而是範圍（散在約 20 檔）、每句都要判讀括號是不是程式片段（如 `ceil(金額/1,000)`、`RSI(14)`），以及這一輪「不動已驗收項目」的原則 → 提為 Q1；**同日裁定分批，兩批皆已完成（§十.5），後端掃描現為 0 處**。

### 10.3 L8 — 單一定義

- 推導邏輯（zh 名稱＋依綁定市場推得的**實際交易幣別**，`markets` 缺時才退回 `ccy`）從 `input.js` 搬到 `names.js::pdNames.accountOption(a)`，與它裝飾的名稱同一個檔；`input.js::accountLabel` 改為委派（三個已驗收下拉輸出逐字相同）；`cash.js` 的 `#cfx-account`／`#cm-account` 原本串接 `settlement_ccy`，改呼叫同一定義。
- 守門：`test_account_name_single_source.py` 新增測試——`names.js` 必須擁有 `accountOption`，`cash.js`／`input.js` 必須呼叫它、不得自帶市場→幣別表、`settlementCcy(a)` 只能作為同一運算式的 fallback；e2e `test_cash_withdraw_estimate_flow.py` 斷言兩個下拉的 moomoo_my 為「Moomoo MY（USD／MYR）」、tw_broker 為「台灣券商（TWD）」。

### 10.4 §9.5 的建議（報價日期往前推的測試開關）

見 §9.5 下方回覆：不加開關，三項以既有隔離測試（老化夾具）覆蓋。

### 10.5 待你決策（Q1，已裁定並執行）

| # | 問題 | 選項 | 建議 |
| --- | --- | --- | --- |
| Q1 | 後端 zh 字串尚有 **96 處**半形標點：57 處使用者可見訊息（`validate.py` 20、`ledgers.py` 12、`dashboard.py` 6、`cash.py` 3、其餘散在 12 檔）、29 處更新紀錄目錄（`whatsnew.py`）、10 處 regex／提示詞（不需處理） | (a) 一併掃並加後端 AST 守門（約 30 檔，需同步更新逐字釘住的契約測試與使用手冊）；(b) 不動，只守前端；(c) 分批：先掃使用者會在 toast／確認框看到的 `api/routers/*` 與 `data_ingestion/validate.py`，再看其餘 | **(c)**——使用者可見面優先，每批可獨立驗收，且不把手冊逐字比對的更新混進這一輪 |

> **裁定（2026-09-17）：採分批，先掃 57 處可見訊息，然後 commit 到分支。**
> **執行結果（第一批）**：57 處全部改為全形（`validate.py` 20、`ledgers.py` 12、`dashboard.py` 6、`cash.py` 3、`csv_import.py`／`manual.py` 各 2、`fees.py` 2、`import_templates.py` 2、其餘 8 檔各 1）；連動更新 6 處逐字釘住的測試與 `.claude/rules/markets-and-fees.md` 的引句。**唯一會被程式再解析的字串**是 CSV 範本表頭的 `(選填)` → `（選填）`：匯入端 `canonical_header` 的去註解 regex 本來就同時吃半形／全形，新增測試 `test_every_annotated_header_canonicalizes_to_its_bare_column_in_both_widths` 釘住五種範本的每個表頭在「範本現行寫法」與「舊下載檔的半形寫法」都還原成同一個欄名，數字欄不會錯位；其他 56 處皆為純顯示文字（toast／驗證訊息／確認框／副標／工作摘要），前端只依 `kind`／`code` 判斷、不比對訊息文字（grep 確認）。`wire.py` 的費率公式 `ceil(金額/1,000)` 保留半形並以理由列入 allowlist。
> **守門**：`test_zh_punctuation_fullwidth.py` 新增後端 AST 掃描（字串常量、排除 docstring、f-string 取字面部分）：`shared/whatsnew.py` 列為 `_BACKEND_PENDING`（第二批，須仍違規否則測試失敗）、`official_templates.py`／`variables.py` 以「提示詞非使用者文案」排除、regex 與公式各以理由列入 `_BACKEND_ALLOWED`（過期即失敗）。
> **第二批（同日執行，owner 指示「接著掃第二批 29 處，然後 commit 到分支」）**：`shared/whatsnew.py` 29 個字串（更新紀錄目錄的 title／desc）以 AST 定位字串常量後自動轉換——括號成對且任一側或內容含中文者整對轉全形、`, : ; ? !` 緊鄰中文者轉全形；逐行審閱 diff（46 行）無誤，另補一處跨字串串接的 `MYR,」馬幣`。全部是純顯示文字（設定 › 更新紀錄面板與 ✦ 新功能面板），不被任何程式解析；docs／web 無引用舊字串。守門測試的 `_BACKEND_PENDING` 清空（保留為未來延後項的位置），後端掃描現為 **0 處**。閘門：ruff clean・`mypy --strict --no-incremental` 796 檔 0 問題・目標測試（守門＋`test_whatsnew_api`＋`test_whatsnew`＋`test_db_stats_api`）52 通過・e2e `test_whatsnew_flow.py`＋`test_rebate_inbox_flow.py` 4 通過；依 owner 指示重型閘門於本輪結束一次跑完。


### 10.6 本輪變更檔案

`portfolio_dash/llm_insight/figure_check.py`（母體、第三態、代碼規則）・`insights_store.py`（`prompt_figures` 欄）・`generate.py`（產生端存母體）・`api/routers/insights.py`（讀取端）・`web/insights.html`＋`styles.css`（三種徽章）・`web/names.js`／`input.js`／`cash.js`（L8）・11 個 `web/*` 檔＋`validate.py`＋`ledgers.py`（L5）・測試：`test_figure_check.py`、`test_generate.py`、`test_insights_store.py`、`test_insight_run_api.py`、`test_account_name_single_source.py`、新檔 `test_zh_punctuation_fullwidth.py`、e2e `test_cash_withdraw_estimate_flow.py`／`test_corporate_actions_flow.py`（後者另修一處 `909f0b2` 遺留的英文帳戶名期望值）・`LESSONS_LEARNED.md`（一則：以測試餵給檢核器它在正式環境永遠拿不到的輸入）。・`CHANGELOG.md`（`[Unreleased]` 段：M9 重做、L5／L8 類別修法、Q1 兩批的補記）。

Q1 第一批另動：`digest_service.py`、`dividend_inbox.py`、`api/routers/cash.py`／`input_center.py`／`ledgers.py`、`data_ingestion/csv_import.py`／`manual.py`／`dividend_model.py`／`fees.py`／`import_templates.py`／`validate.py`、`forex/fx_pnl.py`、`ops/notify_dispatch.py`、`portfolio/dashboard.py`、`strategy/whatif.py`；測試 `test_cash_movement_guard_contract.py`、`test_validate_dual_market.py`、`test_cash_import_api.py`、`test_import_template.py`（＋雙寬度表頭測試）、`test_user_messages_are_zh_tw.py`（docstring）、e2e `test_corporate_actions_flow.py`（範本表頭）、`test_zh_punctuation_fullwidth.py`（後端掃描）；`.claude/rules/markets-and-fees.md`（引句）。第二批另動：`shared/whatsnew.py`、`test_zh_punctuation_fullwidth.py`（清空 pending）。閘門：ruff clean・`mypy --strict --no-incremental`（bare 閘門）**796 檔 0 問題**・pytest 非 e2e 全套 **5,223 通過／0 失敗**（第一次跑出 1 個失敗：spec-17 黃金快照 `tests/golden/dashboard_full.json` 的 `fx_complete_reason` 仍是舊標點，只替換該字串、其餘位元組不動後重跑通過）・e2e `test_corporate_actions_flow.py`＋`test_ledger_correction_doors_flow.py`＋`test_cash_withdraw_estimate_flow.py`＋`test_pages_smoke.py` 四檔同跑 54 通過／1 失敗——`test_dashboard_digest_cards_smoke` 收到一次 500，單獨重跑該測試與整檔 `test_pages_smoke.py`（38/38）皆通過，判定為多檔同一 session 的暫時性狀況，與本次改動無關。

### 10.7 commit 與 push（2026-09-17，分支 `feat/corporate-actions`，未併 main）

單一 commit（複驗三項修復＋Q1 第一批，因 `validate.py`／`ledgers.py`／CHANGELOG 同時承載兩者，不拆）：`fix: the audit author's re-verification — M9 checks the numbers the model was fed, L5 and L8 fixed as classes, backend zh punctuation batch 1`；→ **`cbfac0a`**。第二批：`fix(zh): backend punctuation batch 2 — the what's-new catalog` → **`55e0216`**。報告同步：`docs(audit): finish the re-verification sync …` → **`86a6d6d`**。四個 commit（`9c19296`、`cbfac0a`、`55e0216`、`86a6d6d`）已於 2026-09-17 push 到 `origin/feat/corporate-actions`（`7445208..86a6d6d`），未併 main。部署見 §十.8。

### 10.8 demo 部署與站外驗證（2026-09-17，`86a6d6d`）

| 步驟 | 結果 |
| --- | --- |
| 1. 備份 demo DB（`vm_exec.py`，已記錄） | `portfolio-pre-86a6d6d-20260917T074819Z.db`，20,234,240 bytes 與來源相同，`integrity_check` ok；備份時 `insights` 149 列、尚無 `prompt_figures` 欄；交易 56／股利 23 |
| 2. 快轉＋重啟（`vm_exec.py`，已記錄） | demo checkout `git pull --ff-only origin feat/corporate-actions` → HEAD `86a6d6d`；`pyproject.toml` 無變更；重啟 `portfolio-dash-demo`，15 秒後 `/api/health` `{"status":"ok","version":"0.1.28","commit":"86a6d6d"}`；開機遷移已加上 `insights.prompt_figures` 欄，149 列未動 |
| 3. `verify_live --expect-version 0.1.28` | **ALL PASS**（health commit `86a6d6d`、holdings 14、echarts sha 一致） |
| 4. `/api/insights`（148 張卡） | `figure_flags.snapshot`：none 142／checked 6；`unverified_figures` 0 張（尚無新產生的卡）；`unknown_symbols` 只剩 **6883 ×1**（複驗時 48 個實例）；#37 → `snapshot: "none"` |
| 5. 真瀏覽器唯讀走查（本機 headless Chromium 對 demo） | `insights.html`：「無快照可核」×81、「未知代碼」×1（6883）、無「數值待核」，tooltip 為新文案；`cash.html` 換匯／出金入金下拉皆為「Moomoo MY（USD／MYR）」「嘉信 Schwab（USD）」「台灣券商（TWD）」；渲染文字半形緊鄰中文：收件匣 0、設定 0、儀表板 0、新聞庫 6（全在新聞**標題資料**內，如「玉山金(2884)」，屬來源資料非介面文案）；`/api/whats-new` 與 `/history` 回應 0 處 |
| 6. console | 僅一則 401：`login.html` 送出任意帳密的 `POST /api/auth/login`（見下方新觀察）；其餘頁面 0 錯誤 |
| 註 | 第一次以 Chromium 連 demo 遇 `ERR_NAME_NOT_RESOLVED`（curl 同時可解析），5 秒後重試即成功——與 2026-09-16 部署後同類的暫時性狀況 |

> **新觀察（未處理，供複驗時一併判定）— L22 訪客模式的文案與行為不一致**：demo 為訪客模式（`/api/auth/session` → `mode: guest`，所有頁面與 API 不需登入即可使用），登入頁提示寫「任何帳號密碼都能進入」，但實際在該頁送出任意帳密會得到 `POST /api/auth/login` **401**、畫面顯示「帳號或密碼錯誤」（`auth_store.authenticate` 在沒有任何用戶時一律回 False）。訪客可直接開 `index.html` 進入，所以不是阻斷，但提示句與送出結果矛盾。建議二選一：(a) 訪客模式下登入頁改為「直接進入」按鈕、不打 login；(b) 提示改寫為「無需登入，直接前往儀表板」。此為既有行為（`909f0b2` 之前即如此），非本輪改動所致，未在本輪修改。

---

_複驗於 2026-09-17 02:4x（Asia/Taipei）・受測版本 v0.1.28 · 7445208・複驗者：原稽核流程（真實瀏覽器 ＋ API 比對 ＋ 原始碼交叉確認）_
