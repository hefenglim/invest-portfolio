# portfolio-dash 全站功能稽核 — 總結案報告

| 項目 | 內容 |
| --- | --- |
| 受測站台 | demo（合成資料；實際網址在 git-ignored 的部署筆記） |
| 稽核起始 | 2026-09-15 · `v0.1.28 · 85dfafd` |
| 目前版本 | **`v0.1.28 · 8148fcf`**（分支 `feat/corporate-actions`，未併 main；第三次複驗時為 `faed302`，`8148fcf` 為 M9-a 修復，已部署 demo） |
| 稽核方式 | 真實瀏覽器逐頁點擊＋表單送出＋API 回應逐位元比對＋後端原始碼交叉定位 |
| 複驗方式 | 逐項重放原始重現步驟；無法在站上製造的情境明確標示為「原始碼確認」 |
| 涵蓋範圍 | index · trades · dividend-inbox · cash · instruments · insights · pipeline-hub · news · data-center · settings（8 分頁）· login · input/ledger（轉址頁） |

> **本文件已於 2026-09-21 重新整理**：三輪「稽核 → 修復 → 複驗」的逐輪往返已收斂成下方單一清單（問題／修復方式／複驗結果）。逐輪原文（原 §九～§十三 與各輪決策題）保留在 git 歷史（`86a6d6d`、`faed302` 及其前序 commit），需要時可取回。

---

## 一、總驗收結論

| 面向 | 結果 |
| --- | --- |
| 稽核發現問題總數 | **37**（高 1 · 中 9 · 低 27，其中 2 項複驗後判定為非缺陷） |
| 複驗後增列 | **1**（L22b，修復方自行提出、複驗確認成立） |
| **已結案** | **37 / 38** |
| **尚未結案** | **1**（M9-a：數值檢核的尺度表缺「兆」— 根因已定位到行，屬一行等級修法）<br>→ **修復方回覆（2026-09-21）：已修復並部署 demo `8148fcf`，待第四次複驗**，修法與自檢證據見 §四「修復方回覆」 |
| 金額恆等式 | **94 項全過**（10 組總表恆等式 ＋ 14 檔 × 6 項逐檔） |
| 匯出端點 | **15 / 15 → 200**，檔名／Content-Disposition／內容皆正確 |
| 寫入／刪除還原 | **交易・股利・換匯 三條路徑逐位元還原** |
| RWD（390px） | **0 處**橫向溢位（含設定 8 個分頁） |
| 正常操作 console error | **0** |

**結論**：計算核心可信、寫入與重建路徑正確、匯出與錯誤處理紮實；三輪修復沒有任何一項造成回歸。**距離總驗收只差 M9-a 一項**，其驗收條件寫在 §四。

> **修復方回覆（2026-09-21）**：M9-a 已修復（`8148fcf`）並部署 demo。§四 的三項驗收條件修復方皆已自檢通過，其中第 3 項是在部署後直接對 `/api/insights` 逐張比對（175/175 與離線預測一致）。依複驗者建議，#172／#185／#188 三張真陽性已固定為回歸測試語料。§五 五項亦逐項回覆。**請複驗者進行第四次複驗；結案判定與 §八 簽核仍由複驗者決定。**

---

## 二、問題總表

判定欄位說明：**✅ 結案**＝修復後經真實瀏覽器／API 複驗確認・**ℹ 非缺陷**＝複驗推翻原判斷・**⚠ 未結案**＝仍需處理。

### 2.1 高（H）

| # | 問題 | 修復方式 | 複驗結果 |
| --- | --- | --- | --- |
| **H1** | **再平衡試算：目標合計剛好 100.00% 仍顯示「⚠ 超過 100%」**。根因在兩側：前端 `rebalance.js` 以 JS float 算比例，`0.7/100` 序列化成 `"0.006999999999999999"`；後端 `rebalance.py` 的 `over_allocated = Σ > 1` 零容差，Decimal 相加得 `1.000000000000000004` 即亮燈 | 前後端各補一邊：前端改以「整數・千分之一」為狀態，送出字串由整數拼出（不再 `String(float)`）；後端 `over_allocated = Σ > 1 + Decimal("0.0001")`。另加靜態契約測試禁止 `Number(inp.value)/100` 與 `1.0001` 回流 | **✅ 結案** — 重放原始攔截到的 body（Σ=1.000000000000000004）→ `over_allocated: false`；真實 101.00% → `true`（容差沒遮住真超額）。送出 body 已是定點字串 `"0.007"`／`"0.182"`／`"0.030"`。UI：開啟即 100.00% 無警示，改成 101% 才警示 |

### 2.2 中（M）

| # | 問題 | 修復方式 | 複驗結果 |
| --- | --- | --- | --- |
| **M1** | **三組匯率不三角一致**：`USD/TWD`、`USD/MYR`、`MYR/TWD` 各自獨立儲存，落差 **0.0690%**（三組皆為當日新鮮報價，非過期造成）。後果：4,000 MYR 換 983.28 USD 這種報告幣別應為中性的動作，`reporting_total` 憑空少 40.88 TWD | 指定 base＝USD：`pricing/cross.py` 在每次 FX 寫入後推導 `MYR/TWD = USD/TWD ÷ USD/MYR`（`source=derived:USD`，涵蓋整段歷史），抓取清單移除 MYR/TWD；另在 `freshness.fx_triangulation` 回傳三角檢核並於「資料新鮮度」面板揭露 | **✅ 結案** — `fx_triangulation` 回 `gap_pct -0.0000 / ok:true`；MYR/TWD 的 `source` 為 `derived:USD`；面板顯示「MYR/TWD 推導」與落差句。**獨立重算**（以 `currency_view` 反推）落差 **0.000001%**。更新報價回應內含 `derived:USD: MYRTWD`，證實每次抓取後都會重推 |
| **M2** | **洞察管線卡「② 輸入」節點顯示無意義的標的數**：portfolio scope 顯示「0 檔標的」、per_market 顯示「3 檔標的」，底下卻列 14 個代號。根因：`pipeline_status.py::_input` 的 scope-aware 標籤只寫在 ok 分支，warn／info 分支直接用 `len(universe_symbols)`。附帶：portfolio／market scope 的代號清單 AAPL 重複（未依 instrument 去重） | 抽出 `_input_head()`，scope 判斷提到 warn／info／ok **三個分支之前**：per_symbol→「N 檔標的」、per_market→「N 個市場」、portfolio／on_alert→「全持倉」；`insight_service.py::_price_population` 以 `dict.fromkeys` 去重保序 | **✅ 結案** — API 與 UI 皆為「全持倉／13 檔標的／3 個市場／13 檔標的／全持倉」。去重需「有標的缺價」才會顯示在副標，當時 13 檔全新鮮，以原始碼確認 |
| **M3** | **排程器未運行但介面毫無提示**：25 個工作全顯示「啟用」、「下次執行」全為「—」；儀表板「今日摘要」是兩個月前（2026-07-22）的內容卻仍叫「今日」；月度成績只有 1 筆；最後備份停在 2026-07-18 | (a) `/api/scheduler/jobs` 新增 `scheduler: {running, reason}`，排程中心顯示琥珀橫幅；(b) `freshness.scheduler_running`（三態 true／false／null，絕不把「未知」報成「停用」）餵入儀表板「資料新鮮度」；(c) `digest.js` 對超過門檻（日報 > 2 天、週報 > 8 天）的卡片加「已過期・N 天前產生」 | **✅ 結案** — `scheduler: {running:false, reason:"PD_DISABLE_SCHEDULER=1"}`；排程中心橫幅完整；兩張 digest 卡顯示「已過期・56／63 天前產生」＋「排程器未啟動，摘要不會自動更新」 |
| **M4** | **手動交易寫入後「已持有」提示未刷新，且刷新持股打錯帳戶**：在 `tw_broker` 寫入後，刷新請求是 `?account=moomoo_my`，提示仍顯示交易前的股數與均價 | `input.js::afterCommitRefresh` 原本只暖股利表單的 `#d-account`；改為同時暖 `#m-account`（剛寫入的帳戶），回來後若代號 picker 開著就重繪 | **✅ 結案** — 寫入 2884 100 股後，刷新請求為 `?account=tw_broker`，提示即時更新為「200 股 均價 69.20」（9,320＋4,520＝13,840 ÷ 200 ✓）。驗後已刪除該筆、資料還原 |
| **M5** | **帳戶名稱雙軌**：同一頁同時出現「台灣券商」與「TW Broker」。`web/names.js` 是唯一真相來源，但 `cash.js`／`input.js`／`corp-action-form.js` 列在契約測試的 `_PENDING`，收件匣也未納入 | 六支檔案全部接上 `pdNames.account(row.account_id)`（`cash.js`／`input.js`／`corp-action-form.js`／`rebalance.js`／`rebate-inbox.js`／`inbox.js`），`cash.html`／`dividend-inbox.html` 補載 `names.js`，`_PENDING` 清空 | **✅ 結案** — 交易輸入下拉、CSV 匯入預覽、資金管理現金池與換匯帳本、收件匣全為中文帳戶名；四頁掃描 `TW Broker`／`Charles Schwab`／`tw_broker`／`schwab` 共 **0 次** |
| **M6** | **個股詳情抽屜不進 history**：開啟不寫 hash（無法分享／加書籤）、上一頁不關抽屜卻消耗一筆歷史；反向的 `#sym=XXXX` deep link 卻是通的 | `detail.js`：開啟時 `history.pushState('#sym=XXXX')`；`popstate` 時 hash 不再是 `#sym=` 就關閉；✕／Esc／背景關閉時 `history.back()` 把該筆彈掉；←／→ 切換標的用 `replaceState`；deep link 或前進後退開啟的不再 push | **✅ 結案** — 六個情境全過：開啟→`#sym=2330`、history +1；上一頁→抽屜關閉且 hash 清空；✕ 關閉→Δhistory **0**（無幽靈歷史）；deep link 正常；Esc 關閉並清 hash |
| **M7** | **換匯中心：手動填入買入金額後試算提示永遠停在「試算中…」**，即使 `/api/cash/fx-estimate` 已回 200 | 根因是競態而非文案設計：手動填入時 `runEstimate` 的 `!estPristine` 早退丟掉答案卻沒收回提示。`cash.js::markBuyEdited` 改為在 input 事件當下同步改寫說明 | **✅ 結案** — 手動填入後立即顯示「已手動指定買入金額 — 按「重新試算」可依即期匯率回填」 |
| **M8** | **乾跑預檢與管線卡對「價格資料」給出相反判定**：管線卡「② 輸入」是 warn「…缺價/過期」，乾跑預檢 R4 卻「✓ 通過」 | `insight_service.py::_price_state_for()` 收斂為單一 helper，`_gather_facts`／`_preflight_saved`／`_preflight_draft` 三處共用；`missing_price_symbols` 與 `stale_price_symbols` 拆開；新增 `R4_stale_price` info 列（缺價→兩邊 warn；僅過期→兩邊 info，閘門行為不變） | **✅ 結案（現況一致＋原始碼確認）** — 複驗時 13 檔報價全新鮮，兩介面同時為「無警告／R4 通過」。對立情境需人為製造過期資料，未在 demo 上製造，以原始碼確認三處共用 |
| **M9** | **AI 洞察卡數值錯誤未被攔截**：同批資料的兩張卡一張寫「未實現收益 4,290.80 美元」、另一張寫「429.1 萬美元」（**×1000**）；另有幻覺代號「LRDIM (6883)」。系統自己的「AI 戰績」已量化：命中率 13.33%、滾動校準缺口 −46.60% | 讀取時後置檢核 `llm_insight/figure_check.py`：① **母體**改為「實際提示詞內的數字」，存進新欄 `insights.prompt_figures`（不動 `input_snapshot`／快取指紋／Loop-2 評分輸入）② **第三態** `snapshot ∈ {checked, none}`，「無法核對」不再偽裝成「乾淨」③ **代碼判定**改為名單＋形狀規則（`_INDICATOR_RE`），`_CODE_RE` 由 `\d{4}` 放寬到 `\d{4,6}` ④ **徽章**依命中分三種，tooltip 各自對應 ⑤ 卡片**自身預測欄位**（`target_pct`／`horizon_days`／`confidence`）併入母體 ⑥ **跨帳戶合計**由系統供給：`symbol_detail_json` 對多帳戶標的附 `combined` 區塊（合計定義抽到 `portfolio/position_aggregate.py`，單帳戶標的提示詞逐位元不變） ⑦ **（第四輪，`8148fcf`）單位文法**：「兆」與複合單位（百萬／千萬／千億／萬億）、混合數字（1兆1,300億）、`B`／`T`，卡片與提示詞共用同一個掃描器（詳見 §四） | **⚠ 部分結案** — ①～⑥ 全部驗收通過：代碼誤報 **48 → 1**（只剩真的 6883），14 張滿是 MA20/MA60/MA200/RSI(14)/PER 的新卡上**掛零**；舊卡誠實標示「無快照可核」；#175 自身預測標記已消失（讀取時生效，不必重產）；重產後 AAPL 的 `95.0457`／`15,916.00` **不再被標**。**剩 M9-a：尺度表缺「兆」**，詳見 §四<br>→ **修復方回覆**：⑦ 已修並部署 `8148fcf`；部署後 4 張「兆」誤報消失、3 張 ×10 真陽性（#172／#185／#188）保留，待第四次複驗 |

### 2.3 低（L）

| # | 問題 | 修復方式 | 複驗結果 |
| --- | --- | --- | --- |
| **L1** | 持倉表「市值」欄以報告幣別換算值排序卻顯示原幣金額，看起來像沒排序；與相鄰「未實現損益（分幣別）」語意不同卻只有一欄標示 | 排序啟用時表頭顯示「（依報告幣別）」，與「（分幣別）」對稱；tooltip 加註「顯示的是原幣金額」 | **✅ 結案** — 表頭為「市值▼（依報告幣別）」對「未實現損益▼（分幣別）」；tooltip 完整 |
| **L2** | `app.js:1292` 的 keydown handler 未防 `e.target` 非 Element，合成事件可觸發 `TypeError` | `app.js` 與 `detail.js` 的 keydown 都先判斷 `e.target instanceof Element` 再 `.closest` | **✅ 結案** — 重放原始重現（對 `document` 發 Escape／j／k／↓／Enter 合成 keydown）**0 錯誤** |
| **L3** | 錯誤 toast 不會自動消失，也不隨條件解除清掉；同一錯誤會疊層 | `shell.js`：fail／warn 30 秒後自動消失（hover 暫停），同文字取代舊的而非疊加 | **✅ 結案** — 同一錯誤連觸 4 次只留 1 則；自最後一次觸發起約 21–24 秒自動消失 |
| **L4** | 資金管理分頁名「**賬**戶現金」用了非台灣正體字 | 全站「賬」→「帳」：`cash.html`／`cash.js`、後端驗證訊息、what's-new 文案與斷言這些字串的測試 | **✅ 結案** — 資金管理全頁「賬」出現 **0 次**，分頁名為「帳戶現金」 |
| **L5** | 中文文案半形／全形標點混用（收件匣、新聞庫、管線中心等） | 第一輪只改點名的 3 句 → **複驗判為「修了實例、沒修類別」** → 第二輪改為類別修法：前端 **25 處／11 檔**＋後端使用者可見訊息 **57 處**＋更新紀錄目錄 **29 處** 全改全形；新增守門測試 `test_zh_punctuation_fullwidth.py`（HTML 文字節點與屬性、JS 字串常量、後端 AST 掃描，含陽性對照與 allowlist 過期檢查） | **✅ 結案** — 全站渲染文字掃描：儀表板（含再平衡彈窗）、交易帳本 11 分頁、資金管理 3 分頁、觀察清單（含編輯彈窗）、收件匣、設定 8 分頁、登入頁 **介面文案 0 處**；新聞庫 6 處全在**新聞標題來源資料**內。後端端到端實測：未來日期確認框「晚於今日**，**確認無誤**？**」、重跑 `dividend_inbox_scan` 新摘要「14 檔事件已更新**，**1 檔失敗」。僅存 2 處在修復前寫入的歷史 `job_runs` 列，現行程式碼已全形（依裁定「程式修好即通過」） |
| **L6** | 換匯「隱含匯率」的報價基準與帳本列相反（`USD→MYR` 顯示「1 MYR = 0.2500 USD」，帳本卻是「1 USD = …」） | 統一以「較值錢的幣別為 1」報價（rate ≥ 1）；後端 `StoredFxConversion.implied_quote` 決定方向，`/api/ledgers/fx` 多帶 `implied_unit_ccy`／`implied_per_ccy`；表單即時試算同規則 | **✅ 結案** — `MYR→USD` 與 `USD→MYR` 兩個方向都顯示「1 USD = 4.0000 MYR」；API 帶 `implied_unit_ccy: USD`、`implied_per_ccy: TWD`，`implied_rate` ≥ 1 |
| **L7** | 買入幣別設成與賣出相同時，系統靜默改寫賣出幣別、可用餘額也跟著換，無任何提示 | `cfx-to-ccy` 下方新增說明列，下一次不需翻轉時自動隱藏 | **✅ 結案** — 顯示「買賣幣別不可相同 — 已自動將賣出幣別改為 USD，可用餘額也隨之切換」 |
| **L8** | 帳戶下拉以 `settlement_ccy` 標註→「Moomoo MY（USD）」，但該帳戶資金幣別是 MYR、且同時做馬股交易 | 第一輪只改交易帳本三個下拉 → **複驗判為部分** → 第二輪把推導邏輯（zh 名稱＋依綁定市場推得的實際交易幣別）移到 `names.js::pdNames.accountOption()` 單一定義，`input.js` 委派、`cash.js` 的兩個下拉改呼叫同一定義；守門測試禁止自帶市場→幣別表 | **✅ 結案** — 五個下拉（交易／股利／期初庫存／換匯中心／出金入金）字串逐字相同：「Moomoo MY（USD／MYR）」「嘉信 Schwab（USD）」「台灣券商（TWD）」 |
| **L9** | 再平衡預設值＝現權重四捨五入至 1 dp，合計 100.10%，一打開就是錯誤狀態 | `rebalance.js` 改用最大餘額法（Hare）分配預設值；部分缺價時等於原始權重和的 1 dp 四捨五入（不硬湊）；重設／套用上限走同一路徑 | **✅ 結案** — 預設值合計**正好 100.0**，開啟即「目標合計 100.00%」且無超額警示 |
| **L10** | 目標剛好 100% 時後端回 `cash_after: "-1603.23"`（費稅吃掉現金），畫面「現金水位」被 clamp 成 0.00%，透支完全沒揭露 | footer 新增「試算後現金」（帶正負號）；為負時顯示透支警示。現金水位保留 | **✅ 結案** — 顯示「試算後現金 −831 TWD」＋「⚠ 費稅使現金透支 −831 TWD — 請下調部分買進」 |
| **L11** | 跨兩帳戶的 AAPL 與差額過小的 NVDA 動作欄為「—」且無說明 | 後端新增 `summary.holds`（`on_target`／`rounds_to_zero`）與 `summary.excluded_reasons`（`no_price`／`no_rate`／`no_fee_rule`／`no_total`）；前端在「—」旁印原因，footer 列出被排除的標的 | **✅ 結案** — 「—」旁顯示「差額不足最小單位」；跨帳戶的 AAPL 已能產出動作（買 1 股 @ 嘉信 Schwab） |
| **L12** | 台股（TWSE）可輸入 0.5 股並完成試算，缺整數股驗證 | 台股／馬股股數必須是整數：寫入門 `validate_transaction` 新增硬性 `shares_not_integer`（CSV／手動／AI 三個入口共用）；抽屜試算 `strategy/whatif.py` 同規則。美股刻意不擋（fractional 為 deferred，DRIP 本就會產生小數） | **✅ 結案** — 手動輸入 0.5 股→「✕ 台股股數必須是整數（零股以 1 股為單位），目前是 0.5」且確認鍵停用；CSV 匯入同一則訊息 |
| **L13** | 觀察清單快速加入：lookup 失敗時**自動**送出付費的 `POST /api/instruments/ai-resolve`，使用者並未按「AI 辨識」 | 依裁定 (b)+(d)：純代號型輸入（≤ 6 位英數、無名稱）查無時只提示不自動送；含名稱的照舊自動；設定 › AI 與額度新增「代號查無時自動 AI 辨識」開關（`/api/ui-prefs.auto_ai_resolve`，預設開） | **✅ 結案** — 輸入 `ZZZZ9` 全程 `ai-resolve` 呼叫 **0 次**；提示為「查無報價 — 請確認代號與市場；若是用名稱找，可按「AI 辨識」（使用 AI 額度）」；設定頁開關存在 |
| **L14** | 新鮮度徽章只有「部分過期」一種文案：13 檔全過期與 1 檔過期顯示相同 | 依清單計數三態：`全部過期` ／ `部分過期 n/m` ／ `資料新鮮`，tooltip 同步 | **✅ 結案** — 全新鮮時顯示「資料新鮮」；三態齊備（`app.js:78`）。「全部過期」需人為製造過期資料，未在 demo 上製造，以原始碼確認 |
| **L15** | （原判）已實現損益卡缺分幣別小計 | — | **ℹ 非缺陷** — `#realized-footer` 在 390px 可見，內容為「各幣別合計 USD +97.30　TWD +42,005」，與 `realized.by_currency` 相符。**原稽核判斷有誤**（當時只讀了表格元素、未讀卡片頁尾） |
| **L16** | 每次改動日期區間會同時打 6 支帳本 API，即使只有一個分頁可見 | `ledger.js` 改為只抓可見分頁，其餘標記 dirty、切到該分頁才抓；寫入後的 `pdLedgerRefresh(kind)` 抓可見分頁＋剛寫入的分頁 | **✅ 結案** — 改日期區間只打 `/api/ledgers/transactions` **一支**；切到股利分頁才補抓 `dividends`；刪除後也只重抓可見分頁 |
| **L17** | 排程工作摘要輸出未量化的 Decimal：「組合 -0.06720304659217097625472012746」 | `digest_service.py` 改用推播內文同一個 `_signed_pct` helper；儲存的 payload 仍是全精度 | **✅ 結案（端到端）** — 手動執行 `digest_daily` 產生新列「daily digest 2026-09-17: 組合 **+2.53%**」，頁面已無 10 位以上原始 Decimal |
| **L18** | 送出／清除後「手續費 20」「交易稅 0」仍留在欄位，卻同時顯示「股數必須大於 0」紅字 | 寫入成功後手續費／交易稅一併清空；`runManualPreview` 把「股數與價格皆空白」視為 pristine | **✅ 結案** — 寫入後四欄全清空，草稿顯示「—」，無紅字 |
| **L19** | （原判）「刪除並復原」成功後沒有成功 toast | — | **ℹ 非缺陷** — 逐 0.3 秒取樣：「✓ 已復原 帳本紀錄已刪除，項目將重新出現在收件匣」於 **0.3 秒**出現、約 **4.4 秒**自動消失。**原稽核取樣太晚** |
| **L20** | 方向切到「買進」時「當沖（賣出稅 0.15%）」仍顯示且可勾選 | 比照「放空」：買進時隱藏並取消勾選（`trades.html` 加 `#m-daytrade-line`，`input.js::setSide`） | **✅ 結案** — 買進時隱藏且 `checked=false`，賣出時顯示，切回再次隱藏 |
| **L21** | 持倉健診多數卡顯示「信心 0.00」，同時校準曲線說「尚無可比對的卡片」，未區分「信心＝0」與「未提供」 | 信心改以整數百分比顯示；信心 0 加 tooltip「模型自評信心 0%（極低）— 非未提供」；`null` 仍是「純描述・無預測」 | **✅ 結案** — 實測 80%／70%／65%／60%／50%／40%／0%，tooltip 與 null 狀態皆正確 |
| **L22** | 登入頁未說明目前為訪客模式，訪客會誤以為受保護 | `login.html` 載入時 GET `/api/auth/session`，`mode=guest` 時顯示訪客模式提示 | **✅ 結案**（提示句本身的矛盾另列為 L22b） |
| **L23** | 帳戶×市場空交集時表格空白、合計列顯示「0 0」，無空狀態說明 | 無交集時顯示說明列，合計列顯示「—」而非 0 | **✅ 結案** — 「此篩選條件下沒有持倉 — 所選帳戶與市場沒有交集，請調整上方篩選」，合計「—　—」 |
| **L24** | 更新報價成功後 0056 仍停在舊日期，介面無逐檔失敗說明 | 報價工作結果新增 `lagging`（同批成功抓取但最新收盤日仍落後同市場最新日的持倉）；完成 toast 點名並另出一則 warn；狀態仍為 ok | **✅ 結案（機制已具備）** — `/api/actions/refresh-quotes` 回應每個 job 帶 `lagging[]`。複驗當次三個市場皆 `lagging: []`（1155／5225 同為馬股最新收盤日，定義上不落後），文案未觸發屬正確行為 |
| **L25** | 更新報價完成後整頁重載，展開的卡片、篩選、捲動位置全部流失 | `shell.js` 在 reload 前呼叫 `pdBeforeReload`，`app.js` 把持倉篩選／排序、展開中的 `<details>`、捲動位置寫入 sessionStorage，開機讀回一次。仍採整頁重載（避免局部重繪產生兩個版本的資料） | **✅ 結案** — 重載後帳戶 chip、展開的「資料新鮮度」、捲動位置（1200 → 1227）全部還原 |
| **L26** | `0056 元大高股息`（ETF）產業被歸為 Financials，影響產業配置圓餅 | demo DB 先備份再 `UPDATE instruments SET sector='ETF'`；`seed_demo.py` 本來就寫 `sector="ETF"`，是 demo 資料在後續操作中被改掉 | **✅ 結案** — 0056 產業為 ETF；`allocation.by_sector` 出現獨立 `ETF: 277,750`，Financials 由 528,692 降為 247,742 |
| **L27** | 種子資料品質：`dividend_events` 最早 1987-05-11；已實現損益有 4 筆完全相同的 TSLA 與 4 筆近乎相同的 MSFT；2330／5225 亦有重複股利 | 查證後裁定 2(a)：重複列為累積的壓測語料（非 seed 腳本重複執行），先備份後刪除。1987 年那筆是 **AAPL 在 yfinance 的真實配息史**，不動 | **✅ 結案** — 交易 62→**56**、股利 27→**23**、已實現 17→**11** 列、`realized.by_currency.USD` 389.23→**97.30**；重複列已不存在 |

### 2.4 複驗增列

| # | 問題 | 修復方式 | 複驗結果 |
| --- | --- | --- | --- |
| **L22b** | **登入頁文案與行為矛盾**：提示寫「任何帳號密碼都能進入」，但 `auth_store.authenticate`（`auth_store.py:155-161`）在 `auth_users` 為空時走 missing-user 分支、跑一次 dummy scrypt 後 `return False` → 任何帳密都 401「帳號或密碼錯誤」。訪客可直接開 `/index.html`，不是阻斷，是文案錯誤 | 採 (b)＋連結，**不動 `authenticate`**（等時、不洩漏帳號是否存在，是規格 9.1 的行為）：提示改為「…無需登入即可使用，請直接前往儀表板（此頁送出帳密不會通過）…」，下方加「前往儀表板」連結（`#login-guest-link`，只在 `mode=guest` 時顯示） | **✅ 結案** — 提示句與行為一致且主動說明送出不會過；連結可見、`href="index.html"`、點擊後落在 `/index.html`；session 探測仍只呼叫一次；本頁標點掃描 0 處 |

---

## 三、系統面驗證（這些都測了，而且是對的）

### 3.1 金額與成本計算 — 94 項恆等式全過

以 `/api/dashboard` 原始 Decimal 字串獨立重算，三輪複驗皆全過：

- `已實現 + 未實現 = 資產損益`；`含匯兌總損益 = 資產損益 + 本金匯率效果 + 交易與融資成本`
- `Σ 各帳戶市值 = Σ 各市場市值 = Σ 各幣別（報告幣）= Σ 產業配置 = 總市值`
- `Σ 產業權重 = 1`；`Σ 逐檔權重 = 1`
- 逐檔（14 檔 × 6 項）：`市值 = 股數 × 現價`、`未實現 = 市值 − 調整成本`、`資本利得 = 市值 − 原始成本`、`未實現 = 資本利得 + 股利沖減`、`調整成本 = 原始成本 − 股利沖減`、`未實現% = 未實現 ÷ |原始成本|`
- **FX 三角一致性 < 0.001%**（修復前 0.0690%，修復後 0.000001%）

> **「未實現損益%」以原始成本為分母（分子含股利）** 是 `portfolio/dashboard.py:688-698` 記載的刻意決定（調整成本可能 ≤ 0 會翻轉正負號，放空部位亦然），抽屜內明確標示「vs 原始成本」。**判定為正確，非缺陷。**

### 3.2 費稅規則（逐筆手算比對）

| 情境 | 系統值 | 手算 |
| --- | --- | --- |
| 台股賣出 2,600 @ 2,470 | 手續費 9,151／證交稅 19,266／淨收 6,393,583 | 6,422,000×0.1425%=9,151.35→9,151；×0.3%=19,266 ✓ |
| 台股買進 1,000 @ 2,500 | 手續費 3,562／稅 0／總成本 2,503,562 | 2,500,000×0.1425%=3,562.5→3,562 ✓ |
| 當沖賣出 100 @ 45.55 | 稅 6 | 4,555×0.15%=6.83→6 ✓ |
| 零股賣出 0.5 @ 2,470 | 手續費 20（取最低）／稅 3 | 1,235×0.1425%=1.76→min 20；×0.3%=3.7→3 ✓ |
| 賣超 5,000／持有 2,600 | 明確警告＋公司行動補登入口＋成本基礎歸零確認框 | ✓ |

### 3.3 寫入／刪除／重建 — 逐位元還原

| 路徑 | 寫入驗證 | 還原驗證 |
| --- | --- | --- |
| 交易（新增→編輯→刪除） | 市值 +4,555、總報酬 **−20**（＝手續費）、2884 股數 100→200、均價 93.2→69.475 | 刪除後 KPI／持倉／筆數與基準**字串完全相同** |
| 股利（收件匣確認→復原） | 調整成本 −133,630、股利沖減 +133,630、調整均價 −41.50（＝133,630÷3,220）、未實現 +133,630、**總市值不變**、回本率 4.87%→11.67% | 復原後全部還原，項目自動回到收件匣 |
| 換匯（寫入→帳本刪除） | MYR −4,000／USD +983.28、取得均價 4.384338→4.379134、**總報酬完全不變**（符合不變量 6） | 現金池、均價、`reporting_total` 全部逐位元還原 |

### 3.4 其他

- **現金收支滾動餘額**：Moomoo MY／MYR 全 31 列逐列驗算「前一列餘額＋本列金額＝本列餘額」全數相符，期末與 `/api/cash` 一致
- **匯出中心**：15 個端點（CSV／ZIP／HTML／JSONL）三輪皆 200，Content-Disposition、UTF-8 檔名、內容皆正確
- **持倉篩選**：帳戶 × 市場 8 個 chip 與合計列，逐一與 `holdings_subtotals` 吻合
- **排序**：12 欄升冪／降冪皆正確（含跨幣別分組排序）
- **趨勢圖／績效比較**：1M/3M/6M/全部 以 `dataZoom` 正確裁切；市值 ↔ 績效比較、0050 ↔ S&P 500 切換皆正確
- **日期區間篩選**：套用正確；**顛倒區間後端回 400 並顯示「日期區間無效」**；未來區間 200／0 筆；清除還原
- **CSV 匯入預覽**：合法逐列「✓ 可寫入」；非法給出精準訊息（欄位名＋原因）
- **搜尋（⌘K）**：代號／中文名／英文名模糊比對、標示持倉／觀察、查無時導引、方向鍵＋Enter 開抽屜
- **刪除防護**：交易／股利／換匯刪除皆有確認彈窗，「取消」不發任何請求
- **通知中心 demo 防護**：62 個控制項中 48 個 disabled，顯示「示範站不開放通知設定」
- **乾跑預檢**：R1–R8 逐道檢核、零成本、可預覽組裝後提示詞
- **RWD**：390×844 下 `scrollWidth === 390`、零非法溢位；表格以 `overflow-x:auto` 正常內捲；手機側欄開合正常
- **轉址頁**：`input.html` / `ledger.html` 正確轉址至 `trades.html`

---

## 四、尚未結案：M9-a — 數值檢核的尺度表缺「兆」（修復方：已修復並部署 `8148fcf`，待複驗）

### 現象

依裁定重產 13 張卡後，**6 張被標「數值待核」，全部是「市值」**：

| 卡 | 卡片寫的市值 | 母體 `fundamentals_json.market_cap` | 換算 | 判定 |
| --- | --- | --- | --- | --- |
| #186 AAPL | `4.51 兆 USD` | `4514709672075.806` | 4.5147 兆 | ✅ 正確 → **誤報** |
| #187 MSFT | `3.59 兆 USD` | `3588320530555.747` | 3.5883 兆 | ✅ 正確 → **誤報** |
| #189 TSLA | `1.43 兆 USD` | `1433132709532.1418` | 1.4331 兆 | ✅ 正確 → **誤報** |
| #180 2330 | `62.5 兆 TWD` | `62497011861470` | 62.497 兆 | ✅ 正確 → **誤報** |
| #188 NVDA | `5,200 億美元以上` | `5200733149566.65` | **5.2 兆**（＝52,007 億） | ❌ ×10 → **真陽性** |
| #185 8299 | `458.92 億 TWD` | `458917375000` | **4,589.17 億** | ❌ ×10 → **真陽性** |

### 根因（已定位到行）

`portfolio_dash/llm_insight/figure_check.py`：

- **尺度表（第 103–107 行）**：只有 `"千"`／`"萬"`／`"億"`（＋`K`／`M`）—— **沒有 `兆`**
- **`_NUM_RE`（第 168 行）**：單位群組為 `(%|％|千|萬|億|[KkMm](?![A-Za-z0-9]))?` —— 同樣**沒有 `兆`**

用「億」表達的兩張都判對了（而且兩張都真的差 10 倍）；用「兆」表達的四張，數字被讀成裸值 `4.51`／`3.59`／`1.43`／`62.5`，與母體的 `4.51e12` 對不上任何支援的尺度 → 全部落空。

### 建議修法（一行等級）

1. 尺度表補 `"兆": Decimal("1000000000000")`
2. `_NUM_RE` 的單位群組補 `兆`；順帶建議補英文 `B`／`T`（模型也會寫 `4.51T`／`520B`）
3. 補一個以本批 6 張卡為輸入的回歸測試：**4 張「兆」不標、2 張「億」照標**

### 驗收條件（滿足即可總結案）

- [ ] 以 #186／#187／#189／#180 的卡片文字與其母體跑 `check_figures()`，`unverified_figures` 為空
- [ ] 以 #188／#185 同樣跑，`unverified_figures` 仍分別標出 `5,200 億`／`458.92 億`
- [ ] 部署後 `/api/insights` 上這 6 張卡的 `figure_flags` 與上述一致（不需重產，讀取時檢核）

> **補充**：這批抓到的兩個真陽性，與先前確認的 **#172「近 20 日淨買超 497.43 萬」**（母體 `497427` ＝ 49.74 萬）是**同一種錯 —— 單位換算差一個數量級**。人眼看「458.92 億」不會覺得不對，檢核器抓得很準，只是自己的單位表漏了一格。**建議把 #172／#185／#188 三張保留為真陽性回歸語料。**

### 修復方回覆（2026-09-21，commit `8148fcf`，已部署 demo）

**判定：同意根因。** 複驗者定位的兩行完全正確；修的時候照「修類別、不修實例」的原則（L5 那一輪複驗者指出過），先把 demo 上全部 176 張卡掃一遍「數字＋單位表沒有的單位」，結果一格之外還有三個同類缺口，一併修掉：

| 語料裡實際出現的寫法 | 修前讀成 | 修後讀成 | 出現位置 |
| --- | --- | --- | --- |
| `N 兆`（語料共 19 處，例：`4.51 兆`） | 裸值 4.51 | 4.51 × 10¹² | 本批 4 張＋舊卡 |
| `3.66 百萬` | 裸值 3.66 | 3,660,000 | 舊卡 #53 |
| `1.38 千萬` | **1,380**（停在「千」，尺度錯而不只是缺） | 13,800,000 | 舊卡 #56 |
| `1兆1,300億` | `1` 被當成計數略過，只剩 `1,300 億`（差約 9 倍） | **一個數** 1.13 × 10¹² | 舊卡 #54 |

**修復方式**（`portfolio_dash/llm_insight/figure_check.py`）：

1. **單位改成文法而非清單**：中文量級＝前綴（十／百／千）×基底（萬／億／兆），`萬億`＝兆，倍率取字元乘積（`千萬`＝千×萬）。原本的「千」單獨使用仍保留。
2. **「百」只當前綴**：單獨接在數字後面的「百」是「百分位／百分點」，不是單位 —— demo 舊卡 #47 就有「20百分位以下」，若把「百」加進單位表會變成 2,000 被誤標。已加測試固定。
3. **混合數字視為一個數**：`1兆1,300億`、`3億5,000萬` 這種由大到小、中間只有空白（不跨行）的寫法合併成一個值。兩個獨立數字（例如「市值 1兆，成本 1,300億」）不會被合併，已加測試。
4. **拉丁字母**：依建議補 `B`／`T`（只收大寫；小寫 b／t 常是位元／噸）。沿用原本的 lookahead，所以「5 TWD」、ISO 時間戳「2026-07-06T00:12」（語料裡有 12 張卡）都不會被當成單位。
5. **卡片與提示詞共用同一個掃描器**（`_scan_numbers`）：母體 `prompt_figures` 是在產生卡片時從提示詞抽出來的。若兩邊文法不同，新聞裡的「營收 3.82 兆元」會被存成 3.82，引用它的卡片反而被標。現在兩邊只有一套文法。

**跟建議修法（一行）的差別**：一行補「兆」能解掉本批 4 個誤報，但 `千萬` 會繼續被讀成錯的尺度（1,380），`1兆1,300億` 會繼續被拆成兩個數。這兩種寫法目前只出現在沒有母體的舊卡（`none` 狀態，不顯示數值標記），所以今天看不到，但新卡一寫就會重演 M9-a。

**驗收條件自檢**（勾選留給複驗者）：

| 驗收條件 | 修復方自檢結果 | 證據 |
| --- | --- | --- |
| #186／#187／#189／#180 的卡片文字＋母體跑 `check_figures()`，`unverified_figures` 為空 | ✅ 四張皆為 `[]` | ① 用 demo DB 撈出的**實際卡片文字與實際 `prompt_figures`** 離線重跑；② 單元測試 `test_trillion_market_caps_verify_and_the_x10_ones_stay_flagged` 逐字收錄四張卡的市值句 |
| #188／#185 同樣跑，仍分別標出 `5,200 億`／`458.92 億` | ✅ `["5,200 億"]`／`["458.92 億"]` | 同上，同一個測試的另外兩列 |
| 部署後 `/api/insights` 上這 6 張卡的 `figure_flags` 與上述一致（不需重產） | ✅ 一致 | 部署 `8148fcf` 後 `GET /api/insights?limit=500`：175 張卡的 `figure_flags` 與離線預測**逐張相同（0 差異）**；持倉健診分頁實際渲染 `數值待核` ×2，tooltip 為「對不上的數字：5,200 億」「對不上的數字：458.92 億」，AAPL／MSFT／TSLA／2330 無標記，console 0 錯誤 |

**全語料修前／修後比對**（demo DB 176 張卡，逐張比 `figure_flags`）：**只有 4 張改變**，就是 #180／#186／#187／#189 的「兆」誤報消失。其餘 172 張（含 30 張有母體的卡）完全不變；#172、#185、#188 三個 ×10 真陽性與 #173（舊的 AAPL 卡，沒有 `combined` 區塊，重產後的 #186 已乾淨）、6883 代碼標記全部保留。

**舊母體相容性**：本次部署之前產生的卡，母體是用舊文法抽的（若提示詞裡有「N 兆」，存的是裸值 N）。理論上這類卡在新文法下可能多出一個標記，所以上面的全語料比對就是在檢查這件事 —— **實測 0 張受影響**。之後產生的卡，母體與卡片用同一套文法。

**真陽性語料**：依建議保留 #172／#185／#188。三張卡留在 demo DB 不動；另外把它們的原句與母體固定成單元測試（#172 是上一輪的 `test_a_real_scale_error_on_a_fresh_card_stays_flagged`，#185／#188 在本輪的 `_MARKET_CAPS` 表內）。之後任何放寬都會讓測試失敗。

**閘門**：ruff clean・`mypy --strict --no-incremental` 798 檔 0 問題・pytest 非 e2e **5,215 passed / 2 skipped**・e2e 洞察頁子集 2/2（本輪沒有改前端）。新增測試 6 則（兆×6 卡、複合單位、「百」只當前綴、混合數字、B／T 與時間戳、提示詞端同文法）。

**部署**：先備份 demo DB（`portfolio-pre-8148fcf-20260921T142701Z.db`，integrity ok，洞察 176 列、交易／股利 56／23），fast-forward 到 `8148fcf`、重啟，15 秒 health ok；`verify_live --expect-version 0.1.28` ALL PASS。沒有 schema 變更，沒有碰任何帳本列，**本輪 AI 花費 $0**（沒有重產卡片，標記在讀取時生效）。

---

## 五、非缺陷但建議評估的事項

| # | 事項 | 說明與建議 | 修復方回覆（2026-09-21） |
| --- | --- | --- | --- |
| A | **沒有「重產單一標的」的端點** | 抽屜的「重新產生」POST 的是 `/api/insight-types/{id}/run`，會跑整個任務（13 張）；`/api/insight-tasks/{id}/run` 也不收 body。複驗原估 $0.0015、實際花 **$0.0228**。若希望日後能只重產一張，需在 run 端點加 `symbol` 參數 | **同意，本輪不實作，列為待裁定的功能。** 做法會是 `POST /api/insight-types/{id}/run` 收選填的 `symbols`，只縮小 per_symbol 任務的標的範圍；快取鍵本來就是逐張卡計算，不受影響。要一併決定兩件事：排程觸發不能帶這個參數，以及乾跑預檢（R1–R8）要以縮小後的範圍評估。這屬於新功能，依 spec-first 流程需要 owner 先確認。本輪的驗收沒有用到重產（標記在讀取時生效），AI 花費 $0 |
| B | **歷史資料殘留** | 兩處半形標點在修復前寫入的 `job_runs.detail` 列（`digest_weekly` 2026-07-15、`dividend_inbox_scan` 2026-07-03）；142 張舊洞察卡沒有母體、顯示「無快照可核」。現行程式碼皆已正確，依裁定不清理 demo DB | **同意，不動。** 兩列 `job_runs.detail` 是修復前寫入的執行紀錄，142 張舊卡沒有母體，照實顯示「無快照可核」比事後補一個母體誠實。本輪全語料比對也確認這 142 張的狀態沒有變 |
| C | **demo 站為訪客模式，讀寫 API 無認證** | 任何能連到該 hostname 的人可直接呼叫 `POST /api/input/manual/commit`、`DELETE /api/ledgers/*` 等寫入端點。若該位址是透過 Tailscale **Funnel**（公開）而非僅 tailnet 內部可達，建議為 demo 建一組帳密，或在 demo 模式下把寫入端點設為唯讀 | **評估後擴大了範圍，owner 已裁定。** 除了帳本寫入，訪客模式下 **LLM 連線設定**也可以被寫入。它和 7 月安全審查 F1 封住的通知設定是**同一類別**：可以指向外部網址的設定，加上伺服器端保存的憑證。已實際確認 demo 的 4 個模型列都存有金鑰、`auth_users`＝0；`action_log` 自 2026-07-02 起完整（958 列，未達輪替上限），2026-07-05 之後模型設定沒有任何變更，**查無被利用的跡象**。資料來源金鑰可以被覆寫或清空，但送出的目的地是固定的，只有破壞性、不會外洩。**Owner 裁定（2026-09-21）**：demo 的金鑰是 demo 專用，OpenRouter 源頭有設定花費上限，owner 會自行回收金鑰，**本輪不改程式**。prod 是受保護模式，不受影響。帳本寫入仍然開放：demo 是合成資料，每次部署前都有備份。（本 repo 是公開的，所以這裡只寫類別，不寫操作步驟） |
| D | **demo 排程器維持關閉**（裁定 4(a)） | 資料可重現是驗證流程的前提；現在畫面會明說「排程器未啟動」。副作用是報價會再次過期、摘要不會自動更新，屬預期行為 | **同意，維持。** 本輪也沒有打開 |
| E | **AI 校準品質** | 「AI 戰績」自評：量化命中率 13.33%、滾動校準缺口 −46.60%。這是模型能力與提示詞設計的課題，不在本次稽核的修復範圍 | **同意，不在稽核修復範圍。** M9 管的是「數字有沒有被餵過」，不管預測準不準。後者由 AI 戰績（Loop 2）量測 |

---

## 六、demo 站現況與稽核期間造成的變更

**現況（2026-09-21 實測）**：`v0.1.28 · faed302`；持倉 14 檔；排程器關閉（`scheduler_running: false`）；FX 三角落差 `0.0000%`（`ok: true`）；洞察卡 175 張（`checked` 33／`none` 142）；`unverified_figures` 8 張（6 張為 M9-a 的市值批次、#173 舊卡、#172 真陽性）；`unknown_symbols` 1 張（6883，真陽性）。報價因排程器關閉已再次過期，屬預期。

**第四輪部署後（2026-09-21 實測，修復方）**：`v0.1.28 · 8148fcf`；`/api/insights` 提供 175 張卡（`checked` 33／`none` 142，資料庫 176 列，其中 1 張屬已封存任務、不顯示）；`unverified_figures` 只剩 **4 張**：#172、#185、#188（×10 真陽性）與 #173（舊的 AAPL 卡，沒有 `combined`）；`unknown_symbols` 1 張（6883）。帳本與洞察列數沒有變動（交易／股利 56／23，洞察 176）。

**稽核期間的寫入與花費**（皆已還原或屬預期）：

| 動作 | 影響 | 狀態 |
| --- | --- | --- |
| 交易 #67／#68 新增→編輯→刪除 | 驗證寫入／重建路徑 | ✅ 已刪除，KPI 逐位元還原 |
| 股利確認入帳 → 復原（×3） | 驗證收件匣往返 | ✅ 已復原，筆數與總報酬還原 |
| 換匯 #20 寫入 → 刪除 | 驗證 FX 成本池 | ✅ 已刪除，現金池與均價還原 |
| 2412 目標價暫設 100/150 | 驗證觀察清單編輯 | ✅ 已清空 |
| 更新報價 ×3 | 驗證 L24／L25、觸發 M1 推導 | 預期內的資料更新 |
| 手動執行 `digest_daily` | L17 端到端驗證 | 今日摘要更新為 2026-09-17；舊卡仍可由「歷史」查看 |
| 手動執行 `dividend_inbox_scan` | L5 後端字串端到端驗證 | 收件匣待確認 1 → 6 筆（含 1 檔抓取失敗）；**不影響帳本**，可逐筆確認或略過 |
| 產生洞察卡 27 張 | M9 新卡路徑驗證（owner 裁定） | **AI 額度共花 $0.0443**（3.3436 → 3.2994）；其中 #172／#185／#188 建議保留為真陽性語料 |
| （修復方）唯讀查詢 demo DB ×4 | M9-a 全語料離線比對（卡片文字＋`prompt_figures`）；§五 C 評估（模型列是否存有金鑰，只讀是否為空、沒有讀金鑰本身；`auth_users` 數；`action_log` 的 LLM 設定變更） | 全部唯讀（`sqlite3 -readonly`），經 `vm_exec.py` 記錄 |
| （修復方）部署 `8148fcf` | M9-a 修復上線 | 部署前已備份 `portfolio-pre-8148fcf-20260921T142701Z.db`；無 schema 變更、無帳本寫入、AI 花費 $0 |

**未執行的項目**：設定 › 授權用戶「新增用戶」（會立即啟用帳密保護、可能鎖住 demo）；登入頁實際送出帳密（不代為輸入密碼類欄位）；資料來源「測試」連線（未設定金鑰，或本來就標示受阻）。

---

## 七、版本與 commit 軌跡

| 階段 | commit | 內容 |
| --- | --- | --- |
| 稽核基準 | `85dfafd` | 原始稽核對象 |
| 第一輪修復 | `909f0b2` → `486bbb8` → `7445208` | 31 項修復、七題裁定、部署後發現的最後一處英文帳戶名 |
| 第二輪修復 | `cbfac0a` → `55e0216` → `86a6d6d` | M9 重做、L5／L8 類別修法、後端標點兩批、報告同步 |
| 第三輪修復 | `faed302` | M9 自身預測併入母體＋跨帳戶合計進提示詞、L22b 登入頁文案 |
| 第四輪修復 | `8148fcf` | M9-a 單位文法：兆與複合單位、混合數字、B／T，卡片與提示詞共用同一個掃描器（已部署 demo，待複驗） |

分支 `feat/corporate-actions`，已 push；`origin/main` 仍為 `734833b`，**未併 main**。閘門（第三輪）：ruff clean・`mypy --strict` 798 檔 0 問題・pytest 非 e2e 全套 0 failed・e2e 52 passed。閘門（第四輪，`8148fcf`）：ruff clean・`mypy --strict --no-incremental` 798 檔 0 問題・pytest 非 e2e 5,215 passed／2 skipped・e2e 洞察頁子集 2/2（本輪沒有改前端）。

---

## 八、總驗收

| 判定 | 數量 |
| --- | --- |
| ✅ 已結案 | **37** |
| ℹ 其中判定為非缺陷 | 2（L15、L19） |
| ⚠ 尚未結案 | **1**（M9-a）→ 修復方：已修復並部署 `8148fcf`，待第四次複驗 |

**目前無法簽署「0 問題」總驗收**，差 M9-a 一項。該項根因已定位到 `figure_check.py` 的兩行，修法明確、風險極低，驗收條件列於 §四。**修完並通過該三項檢查後，本報告即可結案簽核。**

其餘 37 項全部經真實瀏覽器／API 複驗確認，三輪修復未造成任何回歸：94 項金額恆等式、15/15 匯出端點、三條寫入還原路徑、390px 零溢位、正常操作 0 console error。

> **修復方聲明（2026-09-21）**：M9-a 已依 §四「修復方回覆」修復並部署 demo `8148fcf`，三項驗收條件修復方自檢皆通過（第 3 項是部署後直接對線上 API 比對的結果）。本輪只改 `figure_check.py` 這一個讀取時使用的純函式，全語料比對確認沒有其他卡片受影響，也沒有碰任何帳本資料。**請複驗者進行第四次複驗；本欄的結案判定與簽核由複驗者填寫。**

---

_整理於 2026-09-21（Asia/Taipei）・受測版本 `v0.1.28 · faed302`・稽核與複驗：真實瀏覽器 ＋ API 逐位驗算 ＋ 原始碼交叉定位_

_修復方回覆寫回於 2026-09-21（Asia/Taipei）・部署版本 `v0.1.28 · 8148fcf`・待第四次複驗_
