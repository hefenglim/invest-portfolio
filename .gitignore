# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# --- Virtual environments ---
.venv/
venv/
env/
ENV/

# --- Tooling caches ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Secrets / config (LLM keys, provider settings) ---
.env
.env.*
!.env.example

# --- Private ops / server notes: real VM IP, SSH host keys, operation logs ---
#     (deployment docs in docs/ use placeholders; the REAL host details live here) ---
docs/human_noted/

# --- Personal financial data: NEVER commit the ledger ---
*.db
*.sqlite
*.sqlite3
data/
*.csv.backup
# Raw broker statements / trade exports staged for import (Schwab, Moomoo, TW broker).
# These are the owner's REAL holdings — they were sitting untracked-but-not-ignored on
# 2026-08-06, one `git add -A` away from being published.
sample-trade-data/
broker-statements/

# --- Stress-audit evidence: regenerated every run, never committed or trusted stale ---
scripts/stress_audit/evidence/

# --- Logs ---
*.log

# --- Editor / OS ---
.idea/
.vscode/
.DS_Store
Thumbs.db

# --- Node (only if any frontend tooling is ever added) ---
node_modules/

# --- Working-tree scratch: untracked-but-not-ignored, i.e. one `git add -A` from
#     being committed. Same class as the sample-trade-data incident above; these had
#     accumulated across releases with nothing but discipline keeping them out.
.playwright-mcp/
invest-temp-noted.txt
ntfy_topic_tmp.txt
tpl.json

# --- Design-handoff byproducts: the export tree itself IS tracked (69 files); these
#     two are tool-generated artifacts of it, not source.
docs/design-handoff/ai-portfolio-watcher/project/uploads/
docs/design-handoff/ai-portfolio-watcher/project/.thumbnail

# NOT ignored, deliberately: .claude/skills/demo-cycle/. The other three skills are
# tracked and CLAUDE.md lists .claude/skills/ as a repository artifact, so that one is
# an asset that has simply never been committed — ignoring it would bury it for good.
